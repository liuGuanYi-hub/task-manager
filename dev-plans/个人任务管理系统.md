# 个人任务管理系统开发计划

## 项目概述
创建一个基于命令行的个人任务管理系统，帮助用户高效管理日常任务和项目。

## 技术栈
- **开发语言**: Python 3.8+
- **数据存储**: JSON/SQLite
- **可选扩展**: Web 界面 (Flask/FastAPI)

## 核心功能

### 第一阶段：基础功能
1. 任务 CRUD 操作（创建、查看、更新、删除）
2. 任务优先级设置（高/中/低）
3. 任务状态管理（待办/进行中/已完成）
4. 任务分类/标签系统

### 第二阶段：进阶功能
1. 任务截止日期和提醒
2. 任务搜索和过滤
3. 任务统计和报表
4. 数据导出功能

### 第三阶段：扩展功能
1. 重复任务支持
2. 子任务分解
3. 时间追踪
4. Web 界面或 GUI 界面

## 开发周期
- **总周期**: 4 周
- **每周目标**:
  - 第 1 周：项目架构 + 基础 CRUD
  - 第 2 周：任务管理和分类功能
  - 第 3 周：搜索统计和报表
  - 第 4 周：扩展功能和优化

## Skill 加速点

在开发过程中可以使用 skill 来加速以下环节：

1. **代码生成**: 快速生成基础代码结构
2. **代码审查**: 确保代码质量
3. **问题排查**: 快速定位和解决 bug
4. **文档生成**: 自动生成使用文档
5. **测试编写**: 快速创建单元测试

## 项目结构
```
task-manager/
├── main.py              # 主程序入口
├── models/              # 数据模型
│   ├── task.py
│   └── category.py
├── storage/             # 数据存储
│   └── json_storage.py
├── commands/            # 命令处理
│   ├── create.py
│   ├── list.py
│   ├── update.py
│   └── delete.py
├── utils/               # 工具函数
├── tests/               # 测试文件
└── requirements.txt     # 依赖包
```

## 学习收获
- Python 面向对象编程
- 文件/数据库操作
- 命令行应用开发
- 项目架构设计
- 测试驱动开发

## 下一步
1. 初始化项目结构
2. 创建虚拟环境
3. 安装依赖包
4. 开始第一阶段开发

---

## 2026-08-04 11:39 更新：参考 Plane/Focalboard/WeKan 的分阶段升级路线

### 一、当前基线

本项目已经完成基础任务管理和 Web 扩展，当前实际状态如下：

- 技术栈：Python、Flask、原生 HTML/CSS/JavaScript、JSON 文件存储、pytest。
- 任务模型已有：标题、描述、优先级、状态、创建时间、截止时间、标签和 ID。
- Web 页面已有：任务列表、统计、周报、标签、日历、项目、设置和数据导出。
- 当前“项目”不是独立数据实体，而是由任务标签自动聚合生成。
- 当前日历按 `created_at` 聚合任务，不是按 `due_date` 聚合；这应作为后续修正点。
- 当前测试基线：`python -m pytest tests/ -q`，20 个测试全部通过。

### 二、升级原则

1. 保留现有命令行入口和 Flask 页面，不做一次性重写。
2. 先稳定任务模型、查询规则和数据兼容，再增加看板和保存视图。
3. JSON 继续作为当前默认存储；只有当查询和关系复杂到 JSON 难以维护时，才引入 SQLite。
4. 参考 Plane 的项目/迭代/视图，参考 Focalboard 的卡片界面，参考 WeKan 的看板和状态流转；不直接复制大型项目代码。
5. 每个阶段都必须补测试，并保持原有 20 个测试通过。

### 三、分阶段开发计划

| 阶段 | 目标 | 主要内容 | 完成标准 |
|---|---|---|---|
| 阶段 0：语义校准 | 修正现有功能的业务含义 | 日历同时区分创建日期和截止日期；统一日期解析；确认提醒只针对未完成且临近截止的任务 | 日历、提醒、周报对同一任务的日期解释一致；新增日期边界测试 |
| 阶段 1：任务模型增强 | 为归档和项目化做基础 | 增加 `updated_at`、`completed_at`、`archived`；为 JSON 旧数据提供默认值；保留标签兼容；抽取统一查询/排序方法 | 旧 `tasks.json` 可正常读取；完成、归档、恢复可测试；原有 CLI/Web 不回归 |
| 阶段 2：显式项目与阶段 | 借鉴 Plane 的层级结构 | 增加轻量 `Project` 概念；任务通过 `project_id` 关联项目；保留无项目任务；先用“阶段”替代复杂 Cycles | 项目不再依赖标签生成；项目详情可查看进度；标签仍只承担分类职责 |
| 阶段 3：WeKan 式看板 | 增加直观的状态流转 | 新增 `/board`；按待办/进行中/已完成分列；支持表单提交或轻量 JavaScript 更新状态；移动端保持可用 | 看板和列表数据一致；状态更新后 JSON 持久化；至少覆盖三种状态流转测试 |
| 阶段 4：Focalboard/Plane 式保存视图 | 减少重复筛选操作 | 支持按项目、状态、优先级、标签、截止日期组合筛选；允许保存常用筛选视图 | “高优先级未完成”“本周到期”等视图可保存、读取和删除；查询结果稳定排序 |
| 阶段 5：可靠性与历史 | 避免误删并增强复盘 | 删除改为归档；增加恢复、归档列表、备份导出；补充导入校验和冲突处理 | 归档任务不出现在默认列表；可恢复；导入错误不会破坏现有数据 |
| 阶段 6：可选 SQLite/API | 为数据规模增长做准备 | 先定义存储接口，再实现 SQLiteStorage；保留 JSON 导入导出；按需要增加只读 REST API | JSON/SQLite 可切换；迁移可重复执行；API 与 CLI/Web 使用同一业务服务 |

### 四、推荐实施顺序

当前最应该先做阶段 0 和阶段 1，具体顺序为：

1. 修正日历使用 `created_at`/`due_date` 的语义，并补日期边界测试。
2. 增加归档字段和更新时间字段，确保旧 JSON 数据向后兼容。
3. 抽取统一的任务筛选、排序和统计服务，避免每个路由重复遍历任务。
4. 再增加显式项目模型，避免继续把标签当项目使用。
5. 最后实现看板和保存视图。

### 五、暂不纳入范围

- 多用户登录、角色权限和团队邀请。
- Redis、PostgreSQL、Kubernetes 和实时 WebSocket。
- AI 自动拆解任务、云端同步和复杂第三方集成。
- 一次性迁移到 React/Vue 或复制 Plane 的完整前端架构。

这些能力属于大型协作平台方向，当前阶段引入会增加部署、认证和数据迁移风险，不符合项目“本地可运行、功能可验证”的优先级。

### 六、主要风险与验收检查

- **标签到项目的迁移风险**：同一标签可能被当作分类，也可能被当作项目；迁移时保留标签，不自动删除或强制转换。
- **JSON 兼容风险**：新增字段必须使用 `data.get()` 默认值，不能让旧数据无法读取。
- **日期语义风险**：创建日期适合趋势统计，截止日期适合日历和提醒，两者不能混用。
- **删除风险**：归档、恢复和永久删除要分开，默认操作不应破坏历史数据。
- **回归风险**：每个阶段完成后运行 `python -m pytest tests/ -q`，并手动检查 CLI、任务列表、日历、项目和统计页面。

## 2026-08-04 11:54 更新：阶段 0 和阶段 1 实施结果

### 已完成

- 统一日期解析，支持普通 ISO 时间、日期时间输入和 `Z` 时区格式。
- 日历改为按 `due_date` 展示，周报和统计继续按 `created_at` 统计创建趋势。
- 提醒过滤已完成和已归档任务，只提示未完成任务的临近截止或逾期状态。
- CLI 和 Web 新建/编辑任务均可设置、修改和清除截止时间。
- 任务新增 `updated_at`、`completed_at` 和 `archived` 字段。
- 旧 JSON 缺少新增字段时自动使用兼容默认值。
- 默认任务列表隐藏归档任务，JSON/CSV 导出仍保留归档数据。
- 增加归档、恢复和统一筛选/排序存储方法，为后续归档页面和看板提供基础。
- 新增阶段测试，覆盖旧数据兼容、日期解析、截止日期日历、提醒过滤、归档恢复、CLI/Web 截止时间和查询排序。

### 验证结果

```text
python -m pytest tests/ -q
29 passed
```

### 尚未纳入本轮

- 没有把删除按钮改成归档按钮，避免提前改变原有删除行为；这属于阶段 5。
- 看板、保存视图或 SQLite 尚未纳入；显式项目实体已在阶段 2 实施，后续仍属于阶段 3 至阶段 6。

## 2026-08-04 12:10 更新：阶段 2 实施结果

### 已完成

- 新增轻量 `Project` 实体，项目拥有独立 ID、名称、描述、创建时间和更新时间。
- JSON 存储新增 `projects`、`next_project_id`，旧 `tasks.json` 没有项目字段时仍可读取。
- 任务新增可选 `project_id`；项目存在性在新增和更新任务时校验，未归属项目的任务继续保留。
- 项目列表和项目详情改为读取显式项目，不再根据标签自动生成项目；标签继续承担跨项目分类职责。
- CLI 新增 `create-project`、`list-projects`、`update-project`，任务支持 `--project-id`、`--no-project` 和 `--clear-project`。
- Web 新增项目创建/编辑页面，任务表单支持选择所属项目；项目详情只显示当前项目任务。
- JSON/CSV 导出补充项目数据和任务项目关联，避免导出时丢失关系。
- 新增阶段 2 测试，覆盖实体持久化、项目级隔离、CLI、Web 和导出行为。

### 验证结果

```text
python -m compileall -q models storage commands routes web_app.py main.py
python -m pytest tests/ -q
36 passed
```

### 尚未纳入本轮

- 没有实现保存视图、SQLite/API 或项目删除/归档；看板已在阶段 3 实施。
- 没有把历史标签自动转换为项目，避免改变现有任务的分类语义。

## 2026-08-04 12:25 更新：阶段 3 WeKan 式看板实施结果

### 已完成

- 新增 `/board` 看板入口，按“待办 / 进行中 / 已完成”三列展示未归档任务。
- 看板支持全部项目、指定项目和未归属项目筛选，筛选结果复用存储层项目查询。
- 任务卡片展示优先级、截止时间、项目、标签、描述和任务 ID，保持字段密度清晰。
- 卡片内提供状态选择和提交按钮，可直接把任务移动到其他状态；状态更新继续走 `JSONStorage.update()`，因此会同步维护 `updated_at` 和 `completed_at`。
- 新增响应式三列布局：桌面端并排展示，窄屏横向滚动列，保留看板的状态空间感。
- 看板入口加入主导航，空列、归档任务和项目筛选异常均有明确处理。
- UI 取舍参考 Plane 的工作台分区与视觉层级、Focalboard 的卡片字段组织、WeKan 的列式 Kanban 状态流；没有复制其代码或引入前端框架。

### 验证结果

```text
python -m compileall -q models storage commands routes web_app.py main.py
python -m pytest tests/ -q
41 passed
Flask test client: /board=200, /static/app.css=200
```

### 尚未纳入本轮

- 没有实现拖拽排序、拖拽移动、保存视图和实时协作；当前采用无 JavaScript 的表单状态移动，优先保证可验证和可持久化。
- 当前环境没有 `agent-browser` 或 Playwright，未执行真实浏览器截图检查；已通过模板渲染、路由和静态资源响应检查。

## 2026-08-04 12:38 更新：阶段 4 保存筛选视图实施结果

### 已完成

- 新增 `SavedView` 实体，JSON 顶层数据增加 `saved_views` 和 `next_view_id`，旧数据读取保持兼容。
- 任务查询支持项目、状态组合、优先级、标签、截止日期起止范围、排序字段和倒序条件。
- 新增 `/views` 保存视图工作区，支持临时筛选、结果查看和视图库管理。
- 内置“高优先级未完成”和“本周到期”两个常用筛选预设。
- 支持保存、读取和删除筛选视图；删除视图不会删除任务数据。
- 排序增加任务 ID 作为稳定的次级键，避免相同字段下结果顺序不稳定。
- 日期边界统一解析，截止日期止自动覆盖当天 23:59:59，避免遗漏当天任务。
- 新增阶段 4 测试，覆盖组合筛选、截止日期边界、视图持久化、Web CRUD、预设和非法参数。

### 验证结果

```text
python -m compileall -q models storage commands routes web_app.py main.py
python -m pytest tests/ -q
46 passed
Flask test client: /views=200, /views?preset=week-due=200, /static/app.css=200
```

### 尚未纳入本轮

- 没有实现视图编辑、视图共享、拖拽排序或实时同步；当前保存视图覆盖个人本地筛选复用场景。
- 没有引入 SQLite；JSON 仍是当前默认存储，后续复杂查询增多后再评估迁移。

## 2026-08-04 12:54 更新：阶段 5 归档、恢复、备份与导入校验实施结果

### 已完成

- Web 任务列表的“删除”入口改为归档，CLI `delete-task` 默认归档；只有明确传入 `--permanent` 才会永久删除。
- 新增 `/archive` 归档任务页面，支持查看归档任务并恢复到日常任务列表。
- JSON 存储写入改为同目录临时文件 + 原子替换，降低写入中断导致数据损坏的风险。
- 新增完整备份下载，包含归档任务、项目、保存视图和结构版本信息。
- 设置页面支持 JSON 导入，导入前校验记录结构、ID、任务项目引用、状态和筛选条件。
- 支持 `remap`、`skip`、`replace` 三种 ID 冲突策略，默认重映射冲突 ID 以保留现有数据。
- 导入校验失败或写入失败时回滚内存状态，现有 JSON 文件保持不变。
- 新增阶段 5 测试，覆盖归档恢复、CLI/Web 默认归档、完整备份、非法导入保护、冲突重映射、跳过和覆盖策略。

### 验证结果

```text
python -m compileall -q models storage commands routes web_app.py main.py
python -m pytest tests/ -q
53 passed
```

### 尚未纳入本轮

- 没有实现备份文件的自动定时生成、云端备份或跨设备同步。
- 永久删除仍保留为显式维护操作，默认业务入口不会执行不可逆删除。

## 2026-08-04 13:07 更新：阶段 6 SQLite/API 化实施结果

### 已完成

- 新增 `StorageProtocol`，明确任务、项目、保存视图、归档和 JSON 导入导出的公共存储接口。
- 新增 `SQLiteStorage`，使用 SQLite 表、外键、索引和事务保存任务、项目、保存视图及归档状态；业务方法继续复用 JSONStorage 的规则。
- 新增存储工厂，默认仍使用 JSON；设置 `TASK_MANAGER_STORAGE=sqlite` 后 CLI、Web 和 API 统一切换到 SQLite，路径可通过对应环境变量覆盖。
- 新增 `migrate-sqlite` CLI 命令，将 JSON 数据迁移到独立 SQLite 文件，默认 `replace` 策略可重复执行而不会重复已有 ID。
- 保留原有 JSON 备份、导出和导入能力，SQLite 后端也能复用阶段 5 的校验与冲突处理。
- 新增 `/api/v1` REST API，支持健康检查、任务查询/创建/更新/归档、项目查询/创建及项目详情；API 与 Web/CLI 共用存储业务方法。
- 新增阶段 6 测试，覆盖 SQLite 重启持久化、迁移幂等性、工厂切换、CLI 迁移和 REST API 闭环。

### 验证结果

```text
python -m compileall -q models storage commands routes web_app.py main.py
python -m pytest tests/test_phase_6.py -q
6 passed
python -m pytest tests/ -q
59 passed
```

### 尚未纳入本轮

- SQLite 当前采用内存模型 + 事务快照写入，尚未把复杂筛选全部下推为 SQL 查询。
- API 暂未加入认证、分页、限流和多人协作权限；当前定位仍是本地单用户 API。
- 尚未提供数据库自动备份、云端同步或跨设备冲突合并。

## 2026-08-04 13:12 更新：API 认证与分页实施结果

### 已完成

- 新增可选 Bearer Token 认证，token 通过 `TASK_MANAGER_API_TOKEN` 环境变量注入；未配置时保留本地开发兼容模式。
- 除 `/api/v1/health` 外，配置 token 后的 API 请求都必须携带 `Authorization: Bearer ...`，认证比较使用常量时间比较，错误返回统一 401 结构和 `WWW-Authenticate` 响应头。
- 为任务集合、项目集合和项目详情任务增加 `page`、`page_size` 分页参数，默认 20 条、最大 100 条。
- 统一返回 `page`、`page_size`、`pages`、`total`、`count`、`returned` 元数据，保留 `count` 字段兼容阶段 6 客户端。
- 新增阶段 7 测试，覆盖 token 缺失/错误/正确、健康检查例外、任务分页、项目分页、项目详情分页和非法参数。

### 验证结果

```text
python -m pytest tests/test_phase_6.py tests/test_phase_7.py -q
9 passed
python -m pytest tests/ -q
62 passed
```

### 尚未纳入本轮

- 认证仍是单一静态 token，没有用户、角色、刷新 token 或权限模型。
- 分页当前在业务结果集上切片，SQLite 查询尚未完全下推 `LIMIT/OFFSET`。
- 尚未加入限流、审计日志和 API 版本协商。

## 2026-08-04 13:19 更新：SQLite LIMIT/OFFSET 查询优化结果

### 已完成

- 为存储接口增加 `query_page` 分页查询能力，SQLite 后端直接执行过滤、排序、`COUNT(*)` 和 `LIMIT/OFFSET`。
- 任务查询支持项目、归档、状态组合、优先级、标签、截止日期范围和白名单排序字段，排序的空值顺序与 JSON 后端保持一致。
- 新增 `task_tags` 索引表和 `(tag, task_id)` 索引，按标签查询不再依赖 JSON 文本模糊匹配。
- 项目集合增加数据库分页，项目详情使用数据库分页任务查询和聚合统计，避免为列表统计加载项目全部任务。
- API 检测到 SQLite 分页能力时优先调用 `query_page`，JSON 后端继续走原有全量查询和内存切片。
- 对旧版只有 `tags_json` 的 SQLite 文件增加启动时标签索引回填，保持升级兼容。
- 新增阶段 8 测试，覆盖精确标签过滤、组合条件计数、API 不回退全量查询、项目分页聚合和旧数据库标签回填。

### 验证结果

```text
python -m compileall -q models storage commands routes web_app.py main.py
python -m pytest tests/test_phase_8.py -q
4 passed
python -m pytest tests/ -q
66 passed
```

### 尚未纳入本轮

- SQLite 写入仍采用内存模型加事务快照，后续可以再拆分为按记录更新。
- 复杂全文搜索、统计报表和保存视图查询尚未全部下推 SQL。
- 尚未进行大规模基准测试和并发压测。

## 2026-08-04 22:46 更新：主题模式与深色模式实施结果

### 已完成

- 新增全局主题脚本 `static/theme.js`，支持跟随系统、浅色和深色三种模式，并通过浏览器 `localStorage` 持久化模式选择。
- 设置页面新增四种强调色：默认、玫瑰、海洋和薄荷；强调色同样按浏览器保存，刷新页面后仍能保持。
- 基础模板提供右上角主题快捷按钮，可在系统模式、浅色模式和深色模式之间循环切换，并显示当前模式。
- `app.css` 改为使用 CSS 变量组织页面、窗口、表单、看板、弹窗和设置卡片的颜色，深色模式只切换主题变量即可覆盖主要界面。
- 深色模式增加 `color-scheme`、系统偏好监听、移动端主题操作区适配和主题按钮的可访问名称，降低首屏闪烁和窄屏布局问题。
- 新增阶段 9 测试，验证设置页主题控件、全局主题脚本、深色 CSS 变量和系统媒体查询均能被页面正确加载。

### 验证结果

```text
python -m pytest tests/test_phase_9.py -q
2 passed
python -m compileall -q models storage commands routes web_app.py main.py
python -m pytest tests/ -q
68 passed
```

### 尚未纳入本轮

- 尚未使用真实浏览器做截图级视觉回归，当前通过 Flask 页面渲染、静态资源加载和测试套件验证。
- 主题选择是当前浏览器本地偏好，尚未做跨设备同步或用户账户级主题配置。

## 2026-08-07 10:47 更新：阶段 10 UI 优先开发计划

### 目标

本阶段先把任务管理系统做成“看起来完整、操作路径清晰、适合日常使用”的产品界面，暂不追求业务功能一次性闭环。先确定页面层级、信息密度、交互入口和响应式布局，再逐步把真实数据、存储和 API 接入进去。

### UI 优先原则

- 先设计页面结构、组件状态和交互反馈，再决定数据模型是否需要扩展。
- 允许第一轮使用现有任务数据、模板占位数据或静态演示状态展示界面，不在本阶段新增数据库字段和 API 协议。
- 所有“暂未实现”的操作都要有明确的视觉反馈，例如禁用态、空状态、提示气泡或“即将接入”说明，不能伪装成已经完成。
- 继续沿用当前 CSS 变量、浅色/深色主题和原生 HTML/CSS/JavaScript，避免为了做 UI 原型引入大型前端依赖。
- 每个页面优先适配桌面和移动端两个断点，保证列表、卡片、抽屉、筛选区和导航在窄屏下仍然可读。

### 页面改造顺序

1. **阶段 10.1：Today 工作台**
   - 重新整理首页信息层级，增加“今天、已逾期、即将到期、无截止日期”四个任务区域。
   - 增加顶部快速新建入口、搜索入口、今日任务统计和最近使用的保存视图。
   - 先完成卡片、空状态、加载态、无结果态和深色模式样式；今日任务的真实筛选逻辑后置。

2. **阶段 10.2：任务详情抽屉**
   - 设计从列表、看板和今日工作台打开的统一右侧详情抽屉。
   - 详情区预留标题、描述、状态、优先级、项目、标签、截止日期、子任务、备注和操作历史区域。
   - 第一轮只完成展示、编辑入口、关闭、返回和未保存提示的视觉流程，子任务和历史数据后置实现。

3. **阶段 10.3：看板视觉交互升级**
   - 保留现有三列布局，优化列头、卡片密度、优先级标记、截止日期提醒和空列状态。
   - 增加拖拽中的占位区域、悬停高亮、移动成功提示和移动端替代操作的视觉状态。
   - 第一轮可以只做拖拽演示和状态反馈，不立即写入任务状态；真实拖拽持久化放到后续功能阶段。

4. **阶段 10.4：搜索、快捷操作和筛选入口**
   - 在全局导航增加搜索框或搜索按钮，设计搜索建议、无结果、历史搜索和清空状态。
   - 设计 `N` 新建、`/` 搜索、`B` 看板、`V` 保存视图等快捷键提示面板。
   - 先完成入口、弹层和键盘提示 UI，搜索索引、命令执行和快捷键绑定后置。

5. **阶段 10.5：移动端与可访问性收尾**
   - 优化移动端底部导航、筛选抽屉、详情抽屉、看板横向滚动和按钮触控尺寸。
   - 补充焦点态、键盘导航、`aria-label`、对比度和减少动画设置。
   - 用真实浏览器截图检查浅色、深色、空数据、长标题和窄屏布局。

### 暂不实现的功能

- 子任务的数据库关系、递归任务规则和提醒调度。
- 看板拖拽后的真实状态持久化和排序持久化。
- 全文搜索索引、命令面板业务执行和快捷键写操作。
- 任务详情操作历史、评论、附件和多人协作。
- API 的新字段、用户权限、跨设备同步和实时协作。

### 阶段验收标准

- 每个 UI 子阶段至少有一个可访问页面或可复用组件，不以静态截图替代真实页面结构。
- 主要页面同时覆盖浅色、深色、空数据、长文本、错误提示和移动端布局。
- UI 原型不破坏现有 CLI、Web 路由、JSON/SQLite 存储和 API 行为。
- 新增的交互占位状态必须明确标注未接入的业务能力，避免后续测试误把演示行为当成真实持久化。
- 每完成一个子阶段，再单独决定对应功能是否进入下一阶段实现。

## 2026-08-07 10:56 更新：阶段 10.1 Today 工作台 UI 实施结果

### 已完成

- 新增 `/today/` 页面和 Today 工作台蓝图，使用演示数据渲染“今天要做、已逾期、接下来、还没有安排日期”四类任务区域。
- 新增今日完成进度、四项摘要指标、快速记录占位区、工作流入口和空状态提示，形成桌面端工作台的完整信息层级。
- 主导航新增“今日”入口，工作台可以跳转到已有看板、保存视图和日历页面。
- Today 页面沿用现有 CSS 变量、浅色/深色主题和按钮风格，新增桌面、平板和窄屏布局规则。
- 所有任务勾选、详情、快速记录操作明确显示为禁用或“即将接入”，本轮不修改 Task 模型、JSON/SQLite、API 和任务状态。
- 新增阶段 10 测试，覆盖页面渲染、UI 预览提示和已有导航链接。

### 验证结果

```text
python -m pytest tests/test_phase_10.py -q
2 passed
python -m compileall -q models storage commands routes web_app.py main.py
node --check static/theme.js
python -m pytest tests/ -q
70 passed
```

### 尚未纳入本轮

- 真实任务筛选、今日/逾期计算、快速新建、任务勾选和详情抽屉尚未接入。
- 未进行真实浏览器截图回归：本机没有现成 `playwright-cli`，为避免触发 C 盘下载和全局安装，当前采用 Flask 页面渲染测试、编译检查和静态资源检查。

## 2026-08-07 11:01 更新：阶段 10.2 任务详情抽屉 UI 实施结果

### 已完成

- Today 工作台任务卡的“详情”按钮现在可以打开右侧任务详情抽屉，展示演示任务的标题、所属分组、状态、优先级、截止信息、项目和标签。
- 抽屉新增描述、子任务和操作历史三个信息区，使用清晰的占位状态承载后续真实功能，不伪装成已经持久化的数据。
- 新增遮罩点击、关闭按钮、Esc 关闭、打开后焦点转移和关闭后焦点回到原按钮的交互。
- 新增 `today.js` 负责抽屉状态和演示数据填充，未修改任务模型、存储接口、API 或现有任务编辑路由。
- 抽屉复用现有 CSS 主题变量，支持深色模式、窄屏全宽展示、属性网格折叠和底部操作区响应式布局。
- 阶段 10 测试增加抽屉 DOM、静态脚本、Esc 关闭逻辑和页面资源加载检查。

### 验证结果

```text
python -m pytest tests/test_phase_10.py -q
3 passed
node --check static/today.js
python -m compileall -q models storage commands routes web_app.py main.py
python -m pytest tests/ -q
71 passed
```

### 尚未纳入本轮

- 抽屉中的保存、编辑、勾选子任务、描述编辑和操作历史仍未接入真实业务。
- 任务列表和看板尚未复用 Today 抽屉；当前先在 Today 页面完成交互验证。
- 未进行真实浏览器截图回归，原因与阶段 10.1 相同：本机没有现成 `playwright-cli`，不安装全局依赖或触发 C 盘下载。

## 2026-08-09 20:21 更新：参考 GitHub 高质量任务管理项目的长期开发路线图

### 长期目标

把当前项目从“已经具备任务、项目、看板、保存视图、归档备份、SQLite/API 和主题模式的个人任务工具”，逐步发展成一个适合日常使用的本地优先任务工作台。借鉴成熟开源项目的产品经验，但不直接复制它们的复杂架构，优先解决个人用户每天都会遇到的任务捕获、安排、执行、复盘和恢复问题。

### 参考项目与吸收边界

| 参考项目 | 值得借鉴的能力 | 本项目的吸收边界 |
| --- | --- | --- |
| [Plane](https://github.com/makeplane/plane) | Work Items、Cycles、Modules、保存筛选视图、相关任务、富文本和分析 | 先实现轻量周期/模块/相关任务和个人视图，不引入团队权限、实时协作和复杂项目治理 |
| [WeKan](https://github.com/wekan/wekan) | 看板列、卡片操作、拖拽反馈、实时界面、空列和移动端替代操作 | 先做原生拖拽、键盘移动、撤销提示和移动端操作，不引入 Meteor 或实时服务 |
| [Super Productivity](https://github.com/super-productivity/super-productivity) | Today 工作台、子任务、项目/标签、时间盒、时间追踪、Pomodoro、统计和本地数据 | 优先做 Today、专注计时、任务耗时和本地统计；外部系统同步放到后期 |
| [Vikunja](https://github.com/go-vikunja/vikunja) | 自托管、本地数据所有权、清晰的 API 和可持续路线图 | 延续 Flask + SQLite/JSON 的轻量自托管路线，先保持单用户，不提前引入多租户 |
| [Focalboard](https://github.com/mattermost-community/focalboard) | 卡片/表格/看板的信息布局、属性展示和视图切换 | 只作界面和信息架构参考；官方仓库已标注不再维护，不作为代码依赖或架构基础 |

### 开发原则

1. **先完成现有 UI 路线，再接真实业务**：阶段 10.3、10.4、10.5 先完成看板、搜索快捷操作、移动端和可访问性，避免在抽屉和页面结构未稳定前反复修改数据模型。
2. **每个能力按四层交付**：页面入口与状态 → 业务规则 → SQLite/JSON 存储 → API、CLI 和回归测试。只完成页面占位时，必须明确标注“UI 预览”或“即将接入”。
3. **优先本地优先和可恢复**：所有新增字段先考虑旧 JSON、旧 SQLite 的兼容和备份恢复；批量操作提供撤销、归档或备份路径，不直接删除用户数据。
4. **任务日期要区分语义**：截止日期表示“必须完成的时间”，安排日期表示“计划在哪天做”，提醒时间表示“何时通知”，不能把三个字段混成一个日期。
5. **每个阶段独立验收**：至少包含一个真实可访问页面、一个最小业务闭环、对应测试和文档更新；未完成的高级能力不阻塞基础任务流程。

### 长期阶段安排

#### 阶段 10.3：WeKan 式看板视觉交互升级

- **目标**：让看板从静态三列变成能表达“准备、进行、完成”的工作流界面。
- **开发内容**：列头统计和折叠、卡片优先级/截止日期信息、拖拽中的占位区、悬停高亮、移动成功提示、空列引导、键盘移动和移动端菜单替代拖拽。
- **依赖**：复用当前 Task 状态、项目隔离和详情抽屉，不新增复杂数据模型。
- **验收**：桌面端拖拽演示、移动端可完成同样的状态移动、深色模式可读、拖拽取消和错误状态有明确反馈。

#### 阶段 10.4：搜索、快捷操作和筛选入口

- **目标**：降低从多个页面找到任务和执行常用操作的成本。
- **开发内容**：全局搜索入口、搜索建议、无结果状态、最近搜索、快捷键提示面板、快速新建和保存视图入口。
- **依赖**：先完成 UI 状态，再接现有 API 分页和 SQLite 查询；搜索字段沿用白名单和参数绑定规则。
- **验收**：搜索入口在导航和窄屏可访问，`/`、`N`、`B`、`V` 等快捷键有冲突保护，输入框聚焦时不误触发全局命令。

#### 阶段 10.5：移动端与可访问性收尾

- **目标**：让 Today、列表、看板和详情抽屉在窄屏、键盘和深色模式下保持可用。
- **开发内容**：底部导航、筛选抽屉、详情抽屉全宽展示、看板横向滚动、触控尺寸、焦点环、`aria-label`、减少动画和对比度检查。
- **依赖**：阶段 10.3 和 10.4 的交互入口稳定后再统一处理。
- **验收**：覆盖浅色/深色、空数据、长标题、错误提示、键盘导航和窄屏布局；有条件时补充真实浏览器截图回归。

#### 阶段 11：任务详情与 Today 真实业务闭环

- **目标**：把当前 UI 抽屉从演示状态升级成日常可用的任务编辑中心。
- **开发内容**：抽屉内编辑标题、描述、状态、优先级、项目、标签、截止日期和安排日期；Today 真实计算今天、逾期、即将到期和无日期任务；快速新建、完成勾选、未保存提示和错误回滚。
- **依赖**：先确认字段语义和 API 请求格式，再扩展模型；列表、看板和 Today 统一使用同一套任务更新规则。
- **验收**：从 Today、列表和看板打开同一任务时数据一致，刷新后仍保留修改，失败时不丢失用户输入，JSON/SQLite 两种存储都通过回归测试。

#### 阶段 12：子任务、重复任务和提醒

- **目标**：吸收 Super Productivity/Vikunja 中适合个人使用的任务分解能力。
- **开发内容**：`parent_id` 子任务关系、子任务完成进度、重复规则、跳过本次、提醒时间、到期处理和归档兼容；先支持有限的每日/每周规则，再评估复杂日历规则。
- **依赖**：阶段 11 的真实编辑闭环、数据迁移脚本、备份/恢复和 API 版本兼容。
- **验收**：父任务和子任务隔离清晰，重复任务不会覆盖历史完成记录，提醒失败不影响任务保存，旧数据升级后仍可查询和导出。

#### 阶段 13：时间盒、专注计时和个人复盘

- **目标**：吸收 Super Productivity 的时间盒、计时和可解释统计，帮助用户真正执行任务，而不只是收集任务。
- **开发内容**：安排时长、开始/暂停/结束计时、Pomodoro/专注模式、每日完成时长、项目耗时、计划与实际耗时对比和周报视图。
- **依赖**：阶段 12 的任务日期语义和稳定的任务状态流转；计时记录独立于任务完成状态保存。
- **验收**：浏览器刷新或关闭后计时状态可恢复，暂停和结束不会重复累计，统计可以按项目/标签/日期过滤并导出。

#### 阶段 14：看板高级操作与日历/时间线

- **目标**：把 WeKan 的高反馈看板和 Plane 的周期/模块思路转成适合个人项目的规划视图。
- **开发内容**：拖拽真实持久化、排序字段、撤销移动、泳道或项目分组、周期/模块、周/月日历、安排日期与截止日期对比、可选任务依赖。
- **依赖**：阶段 11 的统一更新接口和阶段 13 的时间语义；排序和依赖字段必须先设计迁移与索引。
- **验收**：多次拖拽刷新后顺序稳定，日历和列表使用同一套日期数据，依赖形成环时拒绝保存并给出原因，分页和索引性能不回退。

#### 阶段 15：数据所有权、集成和发布能力

- **目标**：保持 Vikunja/本地优先方向的可控性，提升数据迁移和长期使用信心。
- **开发内容**：JSON/SQLite 双向导入导出、备份校验、迁移报告、API 文档、访问令牌轮换、操作审计、可选日历导入导出和外部任务导入适配层。
- **依赖**：所有核心字段和 API 契约稳定；敏感配置只从环境或用户本地配置读取，不写入代码和文档。
- **验收**：导入前预览错误、重复和冲突，失败可回滚，备份可恢复，API 分页/认证/权限边界有测试；不为单用户版本提前引入复杂实时同步。

#### 阶段 16：质量、性能和可维护性收尾

- **目标**：让项目从个人可用进一步达到可持续维护和发布的状态。
- **开发内容**：SQLite 查询和索引复查、API 性能 smoke test、统一错误页、日志脱敏、无障碍回归、浏览器截图、数据迁移演练、README 和部署文档更新。
- **依赖**：阶段 11-15 的主要功能稳定后再做整体收尾。
- **验收**：全量测试、关键页面浏览器验证、旧备份恢复演练、敏感信息扫描、`git diff --check` 和发布前工作区范围检查全部通过。

### 优先级与明确延期

- **P0**：阶段 10.3-10.5、阶段 11 的任务编辑闭环；这是当前最直接的用户价值路径。
- **P1**：阶段 12 的子任务/重复/提醒、阶段 13 的专注计时和基础统计；这些功能会明显提升个人使用频率。
- **P2**：阶段 14 的真实拖拽、周期、日历、依赖；需要更多数据模型和迁移设计，不能为了视觉效果提前堆字段。
- **P3**：阶段 15 的外部集成、多用户权限、实时协作、插件体系、AI 自动规划和独立移动端；在单用户核心流程稳定前不启动。

### 长任务执行方式

每次只推进一个最小阶段，先在本文件记录目标、范围、非目标和验收标准，再修改代码。完成后运行相关测试、更新知识库、单独提交中文 commit，并由用户决定是否 push 和进入下一阶段。这样可以持续吸收 GitHub 项目的优点，同时保持当前 Flask 项目的简单、可恢复和可维护。

## 2026-08-09 20:28 更新：阶段 10.3 WeKan 式看板 UI 实施结果

### 已完成

- 看板增加“交互预览”提示，明确拖拽只更新当前页面，刷新后恢复，避免把 UI 演示误认为真实持久化。
- 看板列增加可访问的折叠/展开按钮、任务计数、拖放提示和空列引导。
- 任务卡增加 `draggable`、`aria-grabbed`、键盘焦点和拖拽状态，支持鼠标拖放到其他状态列。
- 新增 `board.js`，实现拖拽中的透明反馈、目标列高亮、移动后的提示、空列状态更新和状态播报。
- 新增键盘替代操作：聚焦卡片后使用 Enter/空格抓取，左右方向键切换列，Enter 放下，Escape 取消。
- 保留现有状态选择表单作为移动端和无拖拽设备的真实操作入口；本轮拖拽演示不调用后端，不修改 Task、JSON、SQLite 或 API。
- 新增阶段 10.3 测试，覆盖页面标记、脚本资源、拖拽属性、可访问性属性和关键键盘逻辑。

### 验证结果

```text
python -m pytest tests/test_phase_10_3.py tests/test_phase_3.py -q
7 passed
node --check static/board.js
python -m compileall -q models storage commands routes web_app.py main.py
python -m pytest tests/ -q
73 passed
git diff --check
通过
```

### 尚未纳入本轮

- 拖拽后的状态和排序尚未持久化，刷新页面会恢复原始数据。
- 看板还没有复用 Today 的任务详情抽屉；统一任务编辑闭环放到阶段 11。
- 本轮未进行真实浏览器截图回归，当前以 Flask 页面渲染、静态脚本语法、键盘交互代码检查和测试套件验证为主。

## 2026-08-09 20:34 更新：阶段 10.4 搜索与快捷操作 UI 实施结果

### 已完成

- 全局导航新增搜索入口，支持点击打开搜索与命令面板预览。
- 新增任务、项目、标签演示结果区域，输入内容可以在当前页面过滤演示结果，并展示无结果状态。
- 新增快捷操作区和筛选预览区，包含新建任务、看板、保存视图、我的任务、已逾期和本周到期等入口。
- 支持 `/`、`Ctrl/Cmd + K` 打开面板，Esc、遮罩和关闭按钮退出；输入框聚焦时不会误触发全局快捷键。
- 快捷操作和筛选按钮明确保持禁用态，真实搜索索引、命令执行和筛选业务后置，不修改 API、存储或任务数据。
- 新增 `search.js` 和阶段 10.4 测试，覆盖面板结构、演示过滤、空状态和快捷键逻辑。
- 搜索面板沿用现有主题变量，支持深色模式、窄屏单列布局和键盘焦点提示。

### 验证结果

```text
python -m pytest tests/test_phase_10_4.py tests/test_phase_10_3.py tests/test_phase_3.py -q
9 passed
node --check static/search.js
python -m compileall -q models storage commands routes web_app.py main.py
python -m pytest tests/ -q
75 passed
git diff --check
通过
```

### 尚未纳入本轮

- 搜索结果仍是静态演示数据，尚未接入全文索引、API 分页或 SQLite 查询。
- N/B/V 等命令行快捷操作只展示提示，尚未执行真实新建、看板和保存视图动作。
- “我的任务、已逾期、本周到期”筛选仍是禁用态，真实筛选规则后置到任务业务闭环阶段。
- 本轮未进行真实浏览器截图回归，继续使用页面渲染、静态脚本语法和测试套件验证。

## 2026-08-09 20:43 更新：阶段 10.5 移动端与可访问性收尾实施结果

### 已完成

- 基础模板新增跳过导航链接、主要内容锚点和主导航 `aria-current="page"` 状态。
- 新增移动端底部快捷导航，提供任务、今日、新建、看板和日历五个高频入口，并为安全区域预留底部空间。
- 保存视图页面新增移动端筛选按钮和抽屉式筛选面板，支持遮罩、关闭按钮、Esc 关闭和打开后焦点进入第一个筛选控件。
- 看板保持横向滚动，任务移动控件在窄屏提高触控尺寸；Today 任务详情抽屉提高层级，避免被移动端底部导航遮挡。
- 全局补充跳过导航、底部导航、筛选抽屉和关键按钮的可见焦点态，增加 `prefers-reduced-motion` 降低动画设置。
- 新增阶段 10.5 测试，覆盖基础模板、移动端导航、保存视图筛选抽屉、静态脚本和可访问性样式。

### 验证结果

```text
python -m pytest tests/test_phase_10_5.py tests/test_phase_10_4.py tests/test_phase_10_3.py tests/test_phase_10.py tests/test_phase_9.py -q
12 passed
node --check static/mobile-filters.js
python -m compileall -q models storage commands routes web_app.py main.py
python -m pytest tests/ -q
78 passed
git diff --check
通过
```

### 尚未纳入本轮

- 真实浏览器截图和移动设备实测未执行：本机没有 `agent-browser` 或 `playwright-cli`，为避免安装全局依赖和触发 C 盘下载，本轮使用 Flask 页面渲染、DOM 结构、脚本语法和测试套件验证。
- 移动端筛选抽屉目前只负责展示已有筛选表单，筛选业务仍由现有页面提交逻辑负责。
- 阶段 11 才接入任务详情抽屉的真实编辑、Today 真实任务计算和列表/看板统一编辑。

## 2026-08-09 20:52 更新：阶段 11.1 Today 真实数据与任务详情入口

### 本轮目标

- 把 Today 工作台从演示任务切换为当前存储后端中的真实未归档任务。
- 复用既有任务字段和编辑路由，让详情抽屉展示真实属性并提供真实编辑入口。
- 复用既有完成切换逻辑，允许用户在 Today 卡片上直接完成或取消完成任务。

### 本轮范围

- 按截止日期把任务分成“今天要做、已逾期、接下来、还没有安排日期”四组。
- 今日进度、完成率、逾期数、未来任务数和无日期任务数改为动态计算。
- 详情抽屉接入任务描述、项目、标签、状态、截止时间、更新时间和编辑地址。
- 完成切换支持安全的站内 `next` 返回地址，完成后回到 Today 页面；默认入口仍返回首页。
- 不新增任务字段、不改变 JSON/SQLite 表结构、不提前实现子任务、重复任务或提醒模型。

### 验收与验证

- 阶段 11.1 测试覆盖真实数据分组、归档隔离、已完成逾期任务隐藏、详情属性和完成切换回 Today。
- 相关测试：`12 passed`。
- Python 编译检查、`today.js` Node 语法检查和 `git diff --check` 均通过。
- 全量测试需要在本轮最后复跑，确保阶段 0-10 的 JSON、SQLite、API、看板、视图和主题行为不回退。

### 本轮未实现

- 详情抽屉仍通过现有编辑页保存，尚未提供抽屉内联编辑。
- 子任务、重复任务、提醒、专注计时和真实拖拽持久化继续留在后续阶段。
- 真实浏览器截图仍受本机缺少 `agent-browser` / `playwright-cli` 限制，本轮不安装浏览器依赖。

## 2026-08-09 20:58 更新：阶段 11.2 任务详情抽屉内联编辑

### 本轮目标

- 把阶段 11.1 的真实详情展示推进为可保存的抽屉内联编辑。
- 复用现有 `/task/<id>/update` 表单协议和 Task 字段，不增加数据库字段或新的存储迁移。
- 保存后回到 Today，保持处理任务的连续工作流。

### 本轮完成

- 详情抽屉新增标题、描述、优先级、状态、截止时间、所属项目和标签编辑控件。
- Today 任务卡通过 `data-*` 提供任务原始值，`today.js` 打开抽屉时完成字段回填和更新地址绑定。
- 保存按钮使用 HTML `form` 属性连接抽屉内表单，复用既有更新路由。
- 更新路由支持受限的站内 `next` 返回地址；外部地址会回退到首页，避免开放重定向。
- 新增阶段 11.2 测试，覆盖表单结构、完整字段持久化、返回 Today 和外部跳转拦截。

### 验证结果

```text
python -m pytest tests/test_phase_11_2.py tests/test_phase_11_1.py tests/test_phase_10.py -q
8 passed
python -m compileall -q models storage routes web_app.py main.py
node --check static/today.js
git diff --check
通过
```

### 尚未纳入本轮

- 抽屉保存后采用页面回跳，尚未做 Ajax/局部刷新和保存成功 Toast。
- 子任务、重复任务、提醒、专注计时和任务历史仍是后续阶段。
- 真实浏览器截图仍受本机没有 `agent-browser` / `playwright-cli` 限制，本轮没有安装下载。

## 2026-08-09 21:05 更新：阶段 11.3 看板与日历统一任务详情编辑

### 本轮目标

- 让 Today、看板和日历三个高频入口共享同一套任务详情抽屉和编辑字段。
- 减少不同视图之间的任务操作分叉，保存后分别回到当前入口。
- 保持现有 Task/Storage、项目隔离和更新路由不变。

### 本轮完成

- 抽取共享模板 `_task_detail_drawer.html`，Today 改为 include 该组件。
- 看板任务卡增加详情入口，传递任务原始字段、项目、更新时间和看板返回地址。
- 日历任务 JSON 增加详情编辑所需字段，弹窗动态生成安全的详情按钮。
- `today.js` 改为事件委托，同时支持 Today/看板静态按钮和日历动态按钮。
- 日历弹窗展示任务标题、描述和元信息时增加 HTML 转义，避免任务内容直接进入 `innerHTML`。
- 新增阶段 11.3 测试，覆盖看板共享抽屉、日历详情数据、脚本资源和更新地址。

### 验证结果

```text
python -m pytest tests/ -q
85 passed
python -m compileall -q models storage commands routes web_app.py main.py
node --check static/today.js
Flask smoke: /today/ 200, /board/ 200, /calendar/ 200
git diff --check
通过
```

### 尚未纳入本轮

- 看板拖拽仍是 UI 预览，尚未把拖拽后的状态持久化到后端。
- 日历仍是月视图弹窗，尚未接入周视图统一编辑和拖拽改期。
- 保存成功后的 Ajax/Toast 和详情历史记录继续留在后续阶段。

## 2026-08-09 21:12 更新：阶段 12.0 截止日期提醒中心

### 变更边界审计

- 当前 Task 模型没有 `parent_id`、重复规则或自定义提醒时间字段。
- JSON 导出版本和 SQLite 初始化仍是现有 schema，尚没有针对父子任务/重复规则的迁移脚本与版本升级流程。
- 因此本轮不擅自修改核心模型、SQLite 表结构或备份格式；先落地完全复用现有 `due_date` 的提醒中心。

### 本轮完成

- 新增 `/reminders/` 提醒中心，分为“已逾期”和“未来 3 天”两组。
- 仅查询待办/进行中且未归档任务；已完成、已归档、无日期和超出窗口的任务不会出现。
- 提醒卡片支持完成、查看统一详情抽屉和进入编辑页，完成后返回提醒中心。
- 主导航新增提醒入口，页面支持浅色/深色主题和移动端单列布局。
- 新增阶段 12.0 测试，覆盖时间窗口、状态/归档过滤和完成回跳。

### 验证结果

```text
python -m pytest tests/ -q
87 passed
Flask smoke: / 200, /today/ 200, /reminders/ 200, /board/ 200, /calendar/ 200, /stats/ 200
python -m compileall -q models storage commands routes web_app.py main.py
node --check static/today.js
git diff --check
通过
```

### 下一阶段的确认门槛

- `parent_id`、子任务进度、重复规则和自定义提醒时间会改变 Task 模型、JSON 兼容、SQLite schema、API payload 与备份导入校验，属于核心数据结构变更。
- 开始该迁移前需要明确：是否接受为 JSON/SQLite 增加 schema 版本和迁移脚本，并允许扩展 API/备份字段。

## 2026-08-09 21:16 更新：阶段 14.1 看板拖拽真实持久化

### 本轮目标

- 把 WeKan 式看板的拖拽和键盘移动从页面预览推进为真实状态流转。
- 复用已有 `/board/task/<id>/status` 路由，不新增任务字段、不改变 JSON/SQLite schema。
- 保存失败时自动恢复原列，并通过看板播报和 Toast 告知用户。

### 本轮完成

- 看板卡片增加状态保存地址，拖拽/键盘移动后使用 `fetch` POST 当前目标状态。
- 状态路由增加可选 JSON 响应模式，保留原有表单提交和重定向兼容。
- 前端为每次保存生成 token，避免连续移动时旧请求失败覆盖新状态。
- 保存失败时将卡片恢复到原列并同步列计数；成功时显示保存反馈。
- 更新看板提示文案，不再把拖拽标记为仅页面预览。
- 新增阶段 14.1 测试，覆盖 JSON 持久化响应和旧表单兼容。

### 验证结果

```text
python -m pytest tests/test_phase_14_1.py tests/test_phase_10_3.py tests/test_phase_3.py -q
9 passed
python -m pytest tests/ -q
89 passed
node --check static/board.js
Flask smoke: / 200, /today/ 200, /reminders/ 200, /board/ 200, /calendar/ 200, /stats/ 200
git diff --check
通过
```

### 尚未纳入本轮

- 拖拽排序仍未持久化，当前只持久化状态列。
- 撤销移动、跨项目批量移动、泳道和周期/模块继续留在后续阶段。
- 子任务/重复任务/自定义提醒时间仍需先获得 schema 迁移确认。

## 2026-08-09 21:21 更新：阶段 10.4.1 全局真实搜索闭环

### 本轮目标

- 把搜索面板从静态演示数据接到当前任务、项目和标签数据。
- 保持跨页面共享搜索入口，同时让结果安全渲染并能进入真实页面。
- 不新增任务字段、不改变 JSON/SQLite schema。

### 本轮完成

- 新增 `/search/` JSON 搜索接口，支持任务标题、描述、项目和标签匹配。
- 搜索默认排除已归档任务，结果包含任务编辑页、项目详情和标签筛选入口。
- 搜索面板改为异步请求真实数据，支持空查询返回最近数据、输入防抖、加载态、错误态和空结果。
- 结果由 JavaScript 使用 DOM `textContent` 创建，避免任务内容直接拼接 HTML。
- 快捷操作改为真实新建任务、看板、保存视图、提醒和本周到期链接。
- 新增阶段 10.4.1 测试，覆盖任务/项目/标签搜索、归档过滤和空查询。

### 验证结果

```text
python -m pytest tests/test_phase_10_4.py tests/test_phase_10_4_1.py -q
4 passed
python -m pytest tests/ -q
91 passed
Flask smoke: / 200, /today/ 200, /reminders/ 200, /search/ 200, /board/ 200, /calendar/ 200, /stats/ 200
node --check static/search.js
node --check static/board.js
node --check static/today.js
git diff --check
通过
```

### 尚未纳入本轮

- 搜索仍按当前任务规模读取并匹配，尚未建立全文索引或分页搜索接口。
- N/B/V 快捷键目前展示真实链接，但尚未实现全局键盘直接执行命令。
- 子任务、重复规则和自定义提醒时间仍受 schema 迁移确认门槛约束。

## 2026-08-09 21:23 更新：阶段 10.4.2 命令面板快捷键执行

### 本轮完成

- 搜索面板的 N/B/V 快捷键现在分别导航到新建任务、看板和保存视图。
- 只有搜索面板打开且焦点不在输入框、选择框或可编辑区域时才执行命令，避免打断关键词输入。
- 快捷入口保留可点击链接，键盘和鼠标两种路径使用同一目标地址。
- 更新阶段 10.4 测试，覆盖命令标记和脚本执行逻辑。

### 验证结果

```text
python -m pytest tests/test_phase_10_4.py tests/test_phase_10_4_1.py -q
4 passed
python -m pytest tests/ -q
91 passed
node --check static/search.js
Flask smoke: / 200, /search/ 200, /today/ 200, /reminders/ 200, /board/ 200, /calendar/ 200
git diff --check
通过
```

### 尚未纳入本轮

- 命令面板尚未支持带参数的创建、批量操作和任务状态命令。
- 子任务、重复规则和自定义提醒时间仍需要 schema 迁移确认。

## 2026-08-09 21:27 更新：阶段 16.1 GitHub Actions 质量门禁

### 本轮目标

- 将当前本地已经稳定执行的测试、Python 编译、JavaScript 语法和差异检查固化为 GitHub Actions。
- 让后续阶段的提交在合并前自动暴露回归，降低 UI/API 持续迭代时的人工漏检风险。
- 本轮只增加 CI 配置，不修改 Task 模型、SQLite schema、备份格式或运行数据。

### 本轮完成

- 新增 `.github/workflows/ci.yml`，在 push 和 pull request 时执行质量检查。
- 使用 Python 3.12 和 `task-manager/requirements.txt`，开启 pip 缓存以减少重复安装时间。
- 固化 `pytest`、`compileall`、Node `--check` 和 `git diff --check` 检查。
- CI 工作目录明确指向实际 Flask 应用目录 `task-manager`，避免仓库根目录与应用根目录混淆。

### 验证结果

```text
python -m pytest tests/ -q
91 passed
python -m compileall -q models storage commands routes web_app.py main.py
node --check static/search.js
node --check static/board.js
node --check static/today.js
git diff --check
CI workflow mechanical assertions passed.
```

### 下一步边界

- 暂不引入 schema 迁移、子任务、重复规则或自定义提醒时间；这些仍需先获得数据库结构变更确认。
- 后续可以在不改数据结构的前提下继续做 API 文档、可观测错误信息和 UI 交互测试；如果要增加浏览器自动化依赖，需要先评估本地磁盘与依赖安装位置。

## 2026-08-09 21:43 更新：阶段 16.2 统一错误页与 API 错误边界

### 本轮目标

- 为未知网页入口提供与现有主题一致、可恢复的错误页面。
- 保持 `/api/*` 的机器可读 JSON 错误契约，避免网页错误页污染 API 客户端响应。
- 隐藏内部异常细节，不向用户页面泄露 traceback 或实现信息。
- 本轮不修改 Task 模型、JSON/SQLite schema、备份格式和已有业务路由。

### 本轮完成

- 在 `web_app.py` 增加 404/500 全局处理器。
- 新增 `templates/error.html`，提供错误状态、回到任务列表和 Today 的恢复入口。
- 增加响应式错误页样式，复用浅色、深色和系统主题变量。
- 未知 API 路径返回 `not_found` JSON；内部 API 错误返回 `internal_error` JSON。
- 新增阶段 16.2 测试，覆盖网页 404、API 404、网页 500、API 500 和错误页样式。

### 验证结果

```text
python -m pytest tests/test_phase_16_2.py -q
5 passed
python -m pytest tests/ -q
96 passed
/route-that-does-not-exist -> 404, html
/api/v1/route-that-does-not-exist -> 404, json
Python compileall、Node --check、git diff --check：通过
```

### 下一步边界

- 真实浏览器截图级错误页回归仍受本机缺少浏览器自动化工具限制。
- 子任务、重复规则、自定义提醒时间和排序字段仍需先确认 schema 版本与迁移策略。

## 2026-08-09 21:45 更新：阶段 16.3 API 发现、契约文档与发布 smoke

### 本轮目标

- 为 `/api/v1` 增加客户端可读取的版本、认证、分页和端点发现信息。
- 把现有 API 的请求字段、错误结构和归档语义整理成仓库内可执行的契约文档。
- 用一组关键网页/API 入口 smoke 检查发布前最小可用面。
- 本轮不修改 Task 模型、JSON/SQLite schema、备份格式或认证算法。

### 本轮完成

- 新增 `GET /api/v1` 和 `/api/v1/`，返回 API 版本、公开入口、Bearer Token 配置名、分页限制和端点列表。
- 发现入口与健康检查在启用 token 时仍保持公开，其他 API 继续遵循已有认证规则。
- 新增 `task-manager/docs/API.md`，记录任务/项目端点、字段值、分页参数、错误响应和安全配置占位符。
- 更新 `task-manager/README.md`，链接到完整 API 契约文档。
- 新增阶段 16.3 测试，覆盖发现入口、认证开启时的公开入口和发布 smoke。

### 验证结果

```text
python -m pytest tests/test_phase_16_3.py -q
3 passed
python -m pytest tests/ -q
99 passed
Release smoke: /、/today/、/reminders/、/board/、/calendar/、/api/v1、/api/v1/health 均返回 200
Python compileall、Node --check、git diff --check：通过
```

### 下一步边界

- API 当前仍是 v1 单用户接口，尚未引入权限角色、token 轮换或操作审计。
- 子任务、重复规则、自定义提醒时间和排序字段仍需先确认 schema 版本与迁移策略。

## 2026-08-09 21:50 更新：阶段 16.4 SQLite 查询基线与 API 性能 smoke

### 本轮目标

- 复查 SQLite 当前索引、SQL 过滤、LIMIT/OFFSET 和 API 分页之间的契约。
- 增加可重复的临时数据性能 smoke，尽早发现 API 回退到全量内存查询或分页失效。
- 本轮只增加验证能力，不新增索引、不修改数据库 schema、不改变生产数据。

### 本轮完成

- 新增阶段 16.4 测试，检查任务索引、标签索引和 SQLite `query_page` 的分页结果。
- 使用 120 条临时任务覆盖项目、状态、标签和多页 API 查询。
- 对 3 页 SQLite API 查询增加宽松性能上限，避免把机器绝对速度误当成业务契约，同时能够捕捉明显的全量回退。
- 记录本机本轮基线：三页分页查询约 `0.0122s`；该数值仅用于本地趋势对比，不作为跨机器 SLA。

### 验证结果

```text
python -m pytest tests/test_phase_16_4.py -q -s
2 passed
SQLite/API pagination smoke: 0.0122s
python -m pytest tests/ -q
101 passed
Release smoke: 主要 Web 页面和 API 入口均返回 200
Python compileall、Node --check、git diff --check：通过
```

### 下一步边界

- 当前仍未增加复合索引或 schema 版本迁移；如要针对更大数据量增加索引，需要单独评估迁移和写入成本。
- API 性能 smoke 不是压力测试，暂不替代多进程并发、真实磁盘和大规模数据基准。

## 2026-08-09 21:55 更新：阶段 16.5 日志脱敏与敏感信息扫描

### 本轮目标

- 为未来日志、诊断和错误上报提供可复用的敏感文本脱敏函数。
- 在 CI 中加入发布前敏感信息扫描，拦截私钥头、常见 token 格式和硬编码 secret assignment。
- 扫描失败只报告文件、行号和规则名，不输出匹配内容，避免安全门禁本身泄露凭据。
- 本轮不读取或提交 `.env`、数据库、JSON 任务数据、Cookie 或真实 token。

### 本轮完成

- 新增 `utils/security.py`，支持 Bearer Token、键值配置、URL 凭据和显式 secret 的统一 `[REDACTED]` 替换。
- 新增 `scripts/security_scan.py`，扫描仓库文本源文件并跳过 `.env`、JSON、数据库、日志和构建缓存。
- GitHub Actions 增加 `python task-manager/scripts/security_scan.py --root .` 门禁。
- 扫描输出只包含相对路径、行号和规则名称；占位符、测试生成 token 和函数生成表达式不会产生误报。
- 新增阶段 16.5 测试，覆盖脱敏、假阳性边界、真实硬编码样例和当前仓库扫描结果。

### 验证结果

```text
python -m pytest tests/test_phase_16_5.py -q
3 passed
python task-manager/scripts/security_scan.py --root .
sensitive scan passed: checked 106 text files
python -m pytest tests/ -q
104 passed
Release smoke: 主要 Web 页面和 API 入口均返回 200
Python compileall、Node --check、git diff --check：通过
```

### 下一步边界

- 当前项目没有集中式日志系统，脱敏函数先作为安全基础设施提供，尚未接入外部日志平台。
- token 轮换、操作审计、权限角色和 schema 迁移仍属于后续阶段，需要单独设计和确认。

## 2026-08-09 22:05 更新：阶段 16.6 发布文档与 UI 运行验证

### 本轮目标

- 让根 README、应用 README 和部署指南准确反映当前已完成的 UI、API、SQLite 和质量能力。
- 修正旧发布说明中的分支、宽范围暂存和 token URL 风险提示。
- 在没有浏览器自动化工具的情况下，建立可复现的页面模板、静态资源和真实 HTTP smoke 证据。
- 本轮不执行旧部署脚本、不安装浏览器依赖、不修改分支或用户数据。

### 本轮完成

- 更新根 README，补充 Today、详情抽屉、看板持久化、提醒、API、主题、CI 和文档入口。
- 更新 `task-manager/README.md`，补充当前能力、API 公开入口、发布前检查和目录结构。
- 更新 `task-manager/DEPLOY.md`，优先推荐明确暂存路径、中文提交、`master` 推送和安全配置注入；旧一键脚本标记为不建议用于当前工作区。
- 新增阶段 16.6 测试，覆盖主要页面共享模板、关键静态脚本可达性和发布文档契约。
- 使用临时本地 Flask 进程执行真实 HTTP 请求，完成页面、API 和静态资源 smoke。

### 验证结果

```text
python -m pytest tests/test_phase_16_6.py -q
3 passed
python -m pytest tests/ -q
107 passed
python task-manager/scripts/security_scan.py --root .
sensitive scan passed: checked 107 text files
HTTP smoke: /、/today/、/reminders/、/board/、/calendar/、/api/v1、/api/v1/health、/static/app.css、/static/search.js 均返回 200
Python compileall、Node --check、git diff --check：通过
```

### 下一步边界

- 本机没有 `agent-browser` 或 `playwright-cli`，未执行真实浏览器截图和点击回归，也未安装下载依赖。
- API 权限、token 轮换、操作审计以及子任务/重复任务 schema 仍需后续阶段确认。

## 2026-08-09 22:15 更新：阶段 13.1 Today 专注计时 UI

### 本轮目标

- 吸收 Super Productivity 的时间盒/专注工作流，先在 Today 工作台提供可用的专注计时入口。
- 支持 25/50/15 分钟预设、开始、暂停、重置和完成状态。
- 刷新页面或短暂离开后能够根据时间戳恢复当前计时。
- 本轮只保存浏览器内的当前计时状态，不增加 Task 字段、不写入 JSON/SQLite，不伪装成历史工时统计。

### 本轮完成

- Today 侧栏新增 Focus Timer 卡片和进度条。
- 新增 `static/focus.js`，实现状态机、倒计时、暂停/恢复、完成、重置和 localStorage 恢复。
- 增加可访问的 `role="timer"`、`aria-live`、进度条属性、按钮状态和减少动画支持。
- 计时运行时禁用时长切换，避免用户在中途改变时间盒造成状态歧义。
- 新增阶段 13.1 测试，确认页面入口、脚本资源、主题样式和“不新增任务字段”的边界。

### 验证结果

```text
python -m pytest tests/test_phase_13_1.py -q
2 passed
python -m pytest tests/ -q
109 passed
node --check static/focus.js
Today、focus.js、app.css smoke：均返回 200
仓库敏感扫描：109 个文本文件通过
Python compileall、git diff --check：通过
```

### 下一步边界

- 当前只保存单个浏览器内的当前计时，不记录任务、项目、标签或历史时长。
- 要实现可查询的每日时长、项目耗时和跨设备恢复，需要先完成 schema 版本、迁移脚本和 API/备份兼容设计。
- 真实浏览器点击和截图仍等待可用的浏览器自动化环境。

## 2026-08-09 22:25 更新：阶段 17.0 数据结构迁移设计与确认门槛

### 本阶段目标

- 针对高赞任务管理项目常见的子任务、提醒、重复任务和工时历史能力，先审计当前 JSON/SQLite/API 数据契约。
- 在不改动现有数据库、JSON 数据文件和模型代码的前提下，形成可回滚、可 dry-run、可验证的 schema 迁移方案。
- 把真正需要用户确认的字段和迁移风险写清楚，避免 UI 需求直接推动不可逆的数据结构变化。

### 本阶段完成

- 新增 `task-manager/docs/SCHEMA_MIGRATION_PLAN.md`，记录 v1→v2 的 `parent_id`、v2→v3 的 `reminder_at`、v3→v4 的受限重复任务、v4→v5 的 `time_entries` 设计顺序。
- 明确迁移 runner 的事务、幂等、版本检查、备份、dry-run、失败回滚和 API/JSON 兼容要求。
- 明确当前版本没有实现 schema runner，也没有修改 `Task`、JSON/SQLite 存储、API 路由或数据文件。

### 当前确认门槛

开始 v1 实施前，需要确认：

1. 是否允许为 JSON/SQLite 引入正式 schema 迁移 runner。
2. 是否接受第一批新增 `Task.parent_id`，并同步加入 API、备份和导入校验字段。
3. 是否接受迁移前自动备份和只读 dry-run CLI。

在确认前，继续保持现有 v1 数据契约，只推进不改 schema 的 UI、文档和质量检查。

### 验证结果

```text
python task-manager/scripts/security_scan.py --root .
git diff --check
```

均通过；本阶段仅新增设计文档和计划记录，未引入运行时代码或数据库变更。

### 下一步边界

- 等待明确确认后，再实施 v1→v2：迁移 runner、`parent_id`、备份/dry-run、导入导出和 API 兼容测试。
- 如果暂不确认 schema 实施，仍可继续开发不依赖新字段的 UI 状态、列表交互和文档质量工作。

## 2026-08-09 22:40 更新：阶段 13.2 Today 快速收集与专注任务绑定

### 本阶段目标

- 把 Today 侧栏的快速记录从静态占位升级为真正可提交的轻量收集入口。
- 参考 Super Productivity 等效率工具，将 Today 任务卡片和本地专注计时连接起来。
- 保持本阶段不新增 Task 字段、不修改 JSON/SQLite schema，不把浏览器计时伪装成历史工时统计。

### 本阶段完成

- 快速收集表单只要求任务标题，复用现有 `/new` 创建逻辑，默认中优先级，并通过安全的 `next=/today/` 返回 Today 工作台。
- Today 页面支持 `N` 快捷键聚焦快速收集输入框；输入框和按钮保持键盘可达。
- 未完成任务卡片增加“专注”入口，当前专注任务显示在计时器卡片中，并可清除。
- 专注任务绑定只写入浏览器 `localStorage`；计时运行时锁定任务切换，重置或切换时长保留当前任务绑定。
- 新增阶段 13.2 测试，覆盖快速收集页面契约、真实创建回跳、本地绑定脚本和样式入口。

### 数据边界

- 本阶段没有新增 `Task` 模型字段，也没有写入 JSON/SQLite/API payload。
- “当前专注任务”保存的是任务 ID 和展示标题快照；如果任务标题后来修改，下一次进入 Today 后以当前页面任务重新选择为准。
- 计时结束不会自动改变任务状态，也不会生成工时历史；后者仍等待 schema 迁移确认。

### 验证结果

```text
python -m pytest tests/test_phase_13_2.py -q
4 passed
python -m pytest tests/ -q
113 passed
node --check static/focus.js
node --check static/today.js
python -m compileall -q .
python task-manager/scripts/security_scan.py --root .
sensitive scan passed: checked 111 text files
git diff --check：通过
```

### 下一步边界

- 真实浏览器点击和截图仍等待可用的浏览器自动化环境；当前已用 Flask test client、静态资源检查和脚本语法检查覆盖可验证部分。
- 子任务、重复任务、自定义提醒时间和持久化工时历史仍受阶段 17.0 schema 确认门槛约束。

## 2026-08-09 22:55 更新：阶段 14.2 日历周视图与真实入口

### 本阶段目标

- 修复日历月视图中指向 `/calendar/week` 的死入口。
- 参考高赞任务管理工具的周计划布局，按截止日期把任务放进连续 7 天的工作列。
- 复用统一任务详情抽屉，保持周视图与 Today、看板、月视图的编辑入口一致。

### 本阶段完成

- 新增 `/calendar/week` 路由，支持 `date=YYYY-MM-DD` 定位周、上周/下周导航和回到本周。
- 周视图以周一为起点，按现有 `due_date` 分组，排除归档任务，并显示当天任务数、优先级、状态和详情入口。
- 新增 `calendar_week.html`，桌面端使用 7 列布局，窄屏保留横向滚动，避免压缩任务卡片内容。
- 月视图的“周视图”改为真实 `<a>` 链接，周视图的“月视图”也会回到当前周对应月份。
- 新增阶段 14.2 测试，覆盖周内任务、归档隔离、跨周隔离、详情抽屉和月视图入口。

### 数据边界

- 本阶段只读取现有 `due_date`，不新增日期字段，不修改 JSON/SQLite schema。
- 周视图暂不支持拖拽改期；拖拽改期需要明确的日期更新交互和更细的浏览器回归测试，留到后续阶段。

### 验证结果

```text
python -m pytest tests/test_phase_14_2.py -q
2 passed
python -m pytest tests/ -q
115 passed
python -m compileall -q .
python task-manager/scripts/security_scan.py --root .
sensitive scan passed: checked 113 text files
git diff --check：通过
```

### 下一步边界

- 真实浏览器截图级回归仍等待可用的浏览器自动化环境；当前已验证 Flask 路由渲染、模板契约和资源加载路径。
- 日历拖拽改期、时间线和任务依赖仍需单独设计交互与验证，不与本阶段混合实现。

## 2026-08-09 23:10 更新：阶段 14.3 日历拖拽改期

### 本阶段目标

- 让月视图和周视图的任务卡片可以拖到另一日期，完成轻量改期。
- 只复用现有 `due_date`，移动日期时保留原截止时间，避免引入新的日期字段。
- 为保存中、成功和失败状态提供清晰反馈，并保留详情编辑作为移动端/键盘替代入口。

### 本阶段完成

- 新增 `/calendar/task/<task_id>/reschedule` POST 接口，支持 JSON 和表单请求。
- 接口校验任务存在、未归档和目标日期格式；改期时保留原有时、分、秒，非法日期不会写入。
- 月视图日期格和周视图日期列增加 drop target，任务卡片增加 draggable 标记。
- 新增 `static/calendar.js`，处理拖拽、JSON 提交、成功刷新和错误状态提示。
- 增加 `calendar.js` 静态资源、拖拽目标和改期接口测试。

### 数据边界

- 本阶段只修改既有 `Task.due_date`，不新增 Task 字段，不修改 JSON/SQLite schema。
- 改期不会改变任务状态、项目、标签或归档属性。
- 移动端触摸拖拽支持仍不宣称已完成；用户可以通过任务详情抽屉的截止时间输入框完成同一操作。

### 验证结果

```text
python -m pytest tests/test_phase_14_3.py -q
3 passed
python -m pytest tests/ -q
118 passed
node --check static/calendar.js
python -m compileall -q .
python task-manager/scripts/security_scan.py --root .
sensitive scan passed: checked 115 text files
git diff --check：通过
```

### 下一步边界

- 真实浏览器拖拽和截图级回归仍等待可用的浏览器自动化环境。
- 时间线、任务依赖、移动端触摸拖拽和批量改期留待后续阶段，不在本轮扩大范围。

## 2026-08-09 23:25 更新：阶段 15.1 任务列表批量完成与批量归档

### 本阶段目标

- 参考高赞任务管理工具的批量操作体验，在任务列表增加全选、选择计数和批量动作。
- 只复用现有 `status` 与 `archived` 语义，保留原有逐条完成、编辑和归档入口。
- 对空选择、非法动作和 JSON 请求提供明确校验，避免静默丢操作。

### 本阶段完成

- 新增 `/tasks/bulk` POST 路由，支持批量完成和批量归档，表单与 JSON 请求均可用。
- 批量完成通过现有 `storage.update` 写入完成状态和完成时间；批量归档通过现有 `storage.archive` 保留数据。
- 任务列表增加页面级全选、单任务复选框、选中数量、批量完成和批量归档按钮。
- 新增 `static/bulk.js`，实现全选同步、按钮禁用态、选择反馈和归档确认。
- 批量操作完成后返回任务列表，并显示服务端实际处理数量。
- 新增阶段 15.1 测试，覆盖页面契约、批量完成、JSON 批量归档和空选择校验。

### 数据边界

- 本阶段没有新增 Task 字段，没有修改 JSON/SQLite schema 和备份格式。
- 批量操作只处理当前仍存在且未归档的任务；重复提交具有幂等行为。
- 原有逐条表单保持不变，批量功能不可用时仍可逐条操作。

### 验证结果

```text
python -m pytest tests/test_phase_15_1.py -q
4 passed
python -m pytest tests/ -q
122 passed
node --check static/bulk.js
python -m compileall -q .
python task-manager/scripts/security_scan.py --root .
sensitive scan passed: checked 117 text files
git diff --check：通过
```

### 下一步边界

- 真实浏览器的全选、归档确认和键盘操作回归仍等待可用的浏览器自动化环境。
- 批量修改优先级、项目和标签可以作为后续扩展，但需要继续控制表单覆盖范围和错误回滚。

## 2026-08-09 23:40 更新：阶段 15.2 批量修改优先级、项目和标签

### 本阶段目标

- 在阶段 15.1 批量完成/归档基础上，补充高频任务属性的批量编辑。
- 批量修改继续复用现有 `Task.priority`、`project_id` 和 `tags` 字段，不引入新的数据结构。
- 对项目存在性、优先级值、空标签和 JSON/表单请求保持一致校验。

### 本阶段完成

- `/tasks/bulk` 新增 `priority`、`project` 和 `tags` 三种动作。
- 任务列表工具栏增加优先级选择、项目选择/清除、标签输入和对应应用按钮。
- `bulk.js` 根据选择数量和字段值控制按钮状态；项目/优先级未选择时阻止无效提交，标签留空表示清除标签。
- 批量标签会去除空值并保持输入顺序去重；项目更新会验证项目实体存在。
- 新增阶段 15.2 测试，覆盖工具栏页面契约、三种属性批量更新和未知项目保护。

### 数据边界

- 本阶段没有新增 Task 字段，也没有修改 JSON/SQLite schema、备份格式或逐条编辑行为。
- 批量项目支持选择已有项目或明确清除项目；不会自动创建项目。
- 批量属性更新只处理当前存在且未归档的任务，服务端校验优先于前端禁用状态。

### 验证结果

```text
python -m pytest tests/test_phase_15_1.py tests/test_phase_15_2.py -q
7 passed
python -m pytest tests/ -q
125 passed
node --check static/bulk.js
python -m compileall -q .
python task-manager/scripts/security_scan.py --root .
sensitive scan passed: checked 118 text files
git diff --check：通过
```

### 下一步边界

- 真实浏览器批量属性选择、键盘焦点和移动端布局回归仍等待可用的浏览器自动化环境。
- 批量修改截止日期、状态以外的复杂字段和操作历史仍需单独设计，避免一次提交覆盖用户未选择的属性。

## 2026-08-09 23:55 更新：阶段 15.3 导入前预览与冲突统计

### 本阶段目标

- 在设置页导入 JSON 备份前，先展示任务、项目、保存视图数量和 ID 冲突。
- 预览必须复用正式导入校验和冲突策略，但不能修改原 JSON/SQLite 数据。
- 保留确认写入路径，并修复成功导入后的页面回跳契约。

### 本阶段完成

- `JSONStorage.preview_import` 使用独立内存副本调用正式 `import_payload`，预览不会调用原存储写入。
- 预览结果显示 incoming 数量、当前 ID 冲突、导入后的数量、跳过数量和重映射数量。
- 设置页新增“先预览”和“确认写入”两个动作，预览失败或校验失败都不改变当前数据。
- 确认写入仍沿用原冲突策略和完整导入逻辑；预览响应明确提示确认时重新选择文件。
- 修复设置路由成功导入时缺少 `redirect` 导入导致 500 的实际问题。
- 新增阶段 15.3 测试，覆盖文件字节不变、页面预览、冲突统计和确认写入。

### 数据边界

- 本阶段没有新增 Task 字段，没有修改 JSON/SQLite schema、备份格式或冲突策略。
- 预览使用深拷贝后的内存存储，不创建临时用户数据文件，不覆盖当前文件。
- 确认写入仍需要用户重新选择文件，避免在服务端保存待确认的用户备份内容。

### 验证结果

```text
python -m pytest tests/test_phase_15_3.py -q
3 passed
python -m pytest tests/ -q
128 passed
python -m compileall -q .
python task-manager/scripts/security_scan.py --root .
sensitive scan passed: checked 119 text files
git diff --check：通过
```

### 下一步边界

- 真实浏览器文件选择、预览后重新选择和确认写入回归仍等待可用的浏览器自动化环境。
- 更复杂的跨版本 schema 迁移、云端备份和冲突合并仍受阶段 17.0 确认门槛约束。
## 2026-08-10 00:10 更新：阶段 15.4 JSON/SQLite 双后端备份恢复 smoke

### 本阶段目标
- 验证 JSON 与 SQLite 后端都支持只读导入预览，预览不会改写原数据文件。
- 验证 JSON→SQLite、SQLite→JSON 以及同后端恢复时，项目、归档任务、标签和保存视图能够保持一致。
- 验证设置页在 SQLite 后端下仍复用同一套预览入口，不引入新的 schema 变化。

### 本阶段完成
- 新增 `tests/test_phase_15_4.py`，覆盖 JSON/SQLite 两种后端的预览文件字节不变检查。
- 新增四种来源/目标后端组合的备份恢复检查，覆盖归档状态、项目关联、标签和保存视图筛选条件。
- 增加 SQLite 设置页 multipart 预览 smoke，确认页面返回成功且数据库文件未被写入。

### 数据边界
- 本阶段不修改 JSON 导出格式，不修改 SQLite schema，不增加 Task/Project/SavedView 字段。
- 预览仍通过内存副本复用正式 `import_payload`，正式写入路径保持不变。

### 验证结果

```text
python -m pytest tests/test_phase_15_4.py -q
7 passed
```

### 下一步边界
- 继续完善导入冲突策略的 UI 解释和恢复演练记录。
- 云端备份、版本化 schema 和更复杂的合并策略仍受阶段 17.0 的显式确认门槛约束。
## 2026-08-09 22:50 更新：阶段 15.5 冲突策略说明与备份恢复演练 UI

### 本阶段目标
- 让 remap、skip、replace 三种导入冲突策略的影响、风险和适用场景在选择时立即可见。
- 增加一套不改动任务数据的恢复演练清单，帮助用户按“下载—检查—预览—写入—回归页面”完成验证。
- 保持恢复记录与业务数据隔离，不增加后端 schema 或敏感数据写入。

### 本阶段完成
- 设置页新增冲突策略说明卡，JavaScript 会根据选择切换说明、风险等级和推荐标签。
- 设置页新增五步备份恢复演练清单，支持当前浏览器本地保存进度和一键重置。
- 新增 `static/settings.js`，并通过设置页专属脚本块加载，避免影响其他页面。
- 新增阶段 15.5 页面契约测试，覆盖策略控件、五步清单、脚本入口和本地存储标识。

### 数据边界
- 本阶段不修改 JSON 导出格式、SQLite schema、任务实体或导入后端逻辑。
- 恢复演练进度仅写入浏览器 `localStorage`，不会写入任务文件、SQLite 数据库或服务器。
- 真实写入仍必须经过现有导入预览和确认按钮。

### 验证结果

```text
python -m pytest tests/test_phase_15_5.py tests/test_phase_15_3.py tests/test_phase_15_4.py -q
12 passed
node --check static/settings.js
```

### 下一步边界
- 继续做真实浏览器级的导入预览/确认回归；当前环境没有可用的浏览器自动化 CLI。
- 更复杂的版本化 schema、云端备份和跨用户恢复仍受阶段 17.0 确认门槛约束。
## 2026-08-09 22:59 更新：阶段 15.6 设置页真实浏览器 smoke

### 本阶段目标
- 在真实浏览器中验证冲突策略说明卡、恢复演练清单和 localStorage 持久化，而不只依赖 Flask 模板测试。
- 检查浏览器控制台和资源请求，避免 UI smoke 在存在 404 的情况下被误判为通过。
- 保留可复查的完成态截图，并让生成的浏览器产物不进入业务数据和 Git 提交。

### 本阶段完成
- 使用 Playwright CLI 打开本地 Flask 设置页，确认页面标题、导航、冲突策略和五步恢复清单真实渲染。
- 切换到 `replace` 策略，确认页面实时显示“高风险”和覆盖提示。
- 逐项勾选五步恢复清单，确认进度从 `0/5` 变为 `5/5`、状态变为“已完成”，并在 localStorage 中看到独立记录。
- 发现并修复浏览器自动请求 `/favicon.ico` 的 404；基础模板增加内联空 favicon 声明，复查后控制台错误为 0。
- 保存完成态截图到 `task-manager/output/playwright/阶段15.6-设置页恢复演练.png`，并将该目录加入忽略规则。

### 数据边界
- 浏览器 smoke 只访问设置页和浏览器存储，没有提交导入表单，不会修改任务 JSON/SQLite 数据。
- 仅新增 favicon 声明、截图产物忽略规则和页面契约断言，不修改业务数据结构。

### 验证结果

```text
python -m pytest tests/ -q
137 passed
node --check static/settings.js
python scripts/security_scan.py --root .
sensitive scan passed: checked 114 text files
Playwright 设置页 smoke：策略切换通过，恢复清单 0/5 → 5/5，通过
Playwright console：Total messages: 0 (Errors: 0, Warnings: 0)
```

### 下一步边界
- 继续扩展真实浏览器 smoke 到导入文件预览和确认写入，但必须使用测试备份文件，避免触碰用户现有数据。
- 更复杂的 schema 版本化和云端恢复仍受阶段 17.0 确认门槛约束。
## 2026-08-09 23:21 更新：阶段 15.7 导入预览与确认写入真实浏览器回归

### 本阶段目标
- 使用隔离的 D 盘 JSON 文件，真实验证设置页的文件选择、预览和确认写入完整链路。
- 证明预览阶段不创建或修改数据文件，确认阶段才写入任务、项目和保存视图。
- 保留可复查的浏览器完成态截图，并补充不依赖浏览器的路由回归测试。

### 本阶段完成
- 在 `output/playwright/phase-15-7-backup.json` 准备仅包含测试数据的备份 fixture。
- 使用备用端口启动 Flask，并通过 Playwright 完成文件选择、预览、重新选择同一文件和确认写入。
- 浏览器预览显示任务/项目/保存视图各 1 条、冲突为 0，确认后页面反馈导入完成各 1 条。
- UTF-8 校验隔离存储，确认任务、项目和保存视图均已落盘且项目关联保持一致；原有 `task-manager/tasks.json` 未被使用。
- 新增 `tests/test_phase_15_7.py`，覆盖预览不创建文件和确认后重新加载恢复。
- 保存完成态截图到 `task-manager/output/playwright/阶段15.7-导入预览确认.png`。

### 数据边界
- 本阶段服务器通过 `TASK_MANAGER_JSON_PATH` 指向 D 盘隔离文件，未触碰现有 `tasks.json`。
- 浏览器未操作真实数据，只上传测试 fixture；隔离存储和截图目录已被忽略，不进入业务提交。

### 验证结果

```text
python -m pytest tests/test_phase_15_7.py tests/test_phase_15_3.py tests/test_phase_15_4.py -q
11 passed
Playwright：文件上传 → 预览 → 重新选择 → 确认写入，通过
Playwright console：0 errors、0 warnings
隔离存储 UTF-8 校验：tasks=1, projects=1, saved_views=1
```

### 下一步边界
- 继续扩展浏览器回归到导入冲突 ID 重映射、skip 和 replace 三种策略。
- 云端备份、版本化 schema 和多用户恢复仍受阶段 17.0 确认门槛约束。
