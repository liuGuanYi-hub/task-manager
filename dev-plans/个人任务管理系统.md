# 个人任务管理系统开发计划

## 项目概述
创建一个基于命令行的个人任务管理系统，帮助用户高效管理日常任务和项目。

## 技术栈
- **开发语言**: Python 3.8+
- **数据存储**: JSON/SQLite
- **可选扩展**: Web 界面 (Flask/FastAPI)

## 核心功能

### 第一阶段：基础功能
1. 任务 CRUD 操作（创建、查看、更新、删除）
2. 任务优先级设置（高/中/低）
3. 任务状态管理（待办/进行中/已完成）
4. 任务分类/标签系统

### 第二阶段：进阶功能
1. 任务截止日期和提醒
2. 任务搜索和过滤
3. 任务统计和报表
4. 数据导出功能

### 第三阶段：扩展功能
1. 重复任务支持
2. 子任务分解
3. 时间追踪
4. Web 界面或 GUI 界面

## 开发周期
- **总周期**: 4 周
- **每周目标**:
  - 第 1 周：项目架构 + 基础 CRUD
  - 第 2 周：任务管理和分类功能
  - 第 3 周：搜索统计和报表
  - 第 4 周：扩展功能和优化

## Skill 加速点

在开发过程中可以使用 skill 来加速以下环节：

1. **代码生成**: 快速生成基础代码结构
2. **代码审查**: 确保代码质量
3. **问题排查**: 快速定位和解决 bug
4. **文档生成**: 自动生成使用文档
5. **测试编写**: 快速创建单元测试

## 项目结构
```
task-manager/
├── main.py              # 主程序入口
├── models/              # 数据模型
│   ├── task.py
│   └── category.py
├── storage/             # 数据存储
│   └── json_storage.py
├── commands/            # 命令处理
│   ├── create.py
│   ├── list.py
│   ├── update.py
│   └── delete.py
├── utils/               # 工具函数
├── tests/               # 测试文件
└── requirements.txt     # 依赖包
```

## 学习收获
- Python 面向对象编程
- 文件/数据库操作
- 命令行应用开发
- 项目架构设计
- 测试驱动开发

## 下一步
1. 初始化项目结构
2. 创建虚拟环境
3. 安装依赖包
4. 开始第一阶段开发

---

## 2026-08-04 11:39 更新：参考 Plane/Focalboard/WeKan 的分阶段升级路线

### 一、当前基线

本项目已经完成基础任务管理和 Web 扩展，当前实际状态如下：

- 技术栈：Python、Flask、原生 HTML/CSS/JavaScript、JSON 文件存储、pytest。
- 任务模型已有：标题、描述、优先级、状态、创建时间、截止时间、标签和 ID。
- Web 页面已有：任务列表、统计、周报、标签、日历、项目、设置和数据导出。
- 当前“项目”不是独立数据实体，而是由任务标签自动聚合生成。
- 当前日历按 `created_at` 聚合任务，不是按 `due_date` 聚合；这应作为后续修正点。
- 当前测试基线：`python -m pytest tests/ -q`，20 个测试全部通过。

### 二、升级原则

1. 保留现有命令行入口和 Flask 页面，不做一次性重写。
2. 先稳定任务模型、查询规则和数据兼容，再增加看板和保存视图。
3. JSON 继续作为当前默认存储；只有当查询和关系复杂到 JSON 难以维护时，才引入 SQLite。
4. 参考 Plane 的项目/迭代/视图，参考 Focalboard 的卡片界面，参考 WeKan 的看板和状态流转；不直接复制大型项目代码。
5. 每个阶段都必须补测试，并保持原有 20 个测试通过。

### 三、分阶段开发计划

| 阶段 | 目标 | 主要内容 | 完成标准 |
|---|---|---|---|
| 阶段 0：语义校准 | 修正现有功能的业务含义 | 日历同时区分创建日期和截止日期；统一日期解析；确认提醒只针对未完成且临近截止的任务 | 日历、提醒、周报对同一任务的日期解释一致；新增日期边界测试 |
| 阶段 1：任务模型增强 | 为归档和项目化做基础 | 增加 `updated_at`、`completed_at`、`archived`；为 JSON 旧数据提供默认值；保留标签兼容；抽取统一查询/排序方法 | 旧 `tasks.json` 可正常读取；完成、归档、恢复可测试；原有 CLI/Web 不回归 |
| 阶段 2：显式项目与阶段 | 借鉴 Plane 的层级结构 | 增加轻量 `Project` 概念；任务通过 `project_id` 关联项目；保留无项目任务；先用“阶段”替代复杂 Cycles | 项目不再依赖标签生成；项目详情可查看进度；标签仍只承担分类职责 |
| 阶段 3：WeKan 式看板 | 增加直观的状态流转 | 新增 `/board`；按待办/进行中/已完成分列；支持表单提交或轻量 JavaScript 更新状态；移动端保持可用 | 看板和列表数据一致；状态更新后 JSON 持久化；至少覆盖三种状态流转测试 |
| 阶段 4：Focalboard/Plane 式保存视图 | 减少重复筛选操作 | 支持按项目、状态、优先级、标签、截止日期组合筛选；允许保存常用筛选视图 | “高优先级未完成”“本周到期”等视图可保存、读取和删除；查询结果稳定排序 |
| 阶段 5：可靠性与历史 | 避免误删并增强复盘 | 删除改为归档；增加恢复、归档列表、备份导出；补充导入校验和冲突处理 | 归档任务不出现在默认列表；可恢复；导入错误不会破坏现有数据 |
| 阶段 6：可选 SQLite/API | 为数据规模增长做准备 | 先定义存储接口，再实现 SQLiteStorage；保留 JSON 导入导出；按需要增加只读 REST API | JSON/SQLite 可切换；迁移可重复执行；API 与 CLI/Web 使用同一业务服务 |

### 四、推荐实施顺序

当前最应该先做阶段 0 和阶段 1，具体顺序为：

1. 修正日历使用 `created_at`/`due_date` 的语义，并补日期边界测试。
2. 增加归档字段和更新时间字段，确保旧 JSON 数据向后兼容。
3. 抽取统一的任务筛选、排序和统计服务，避免每个路由重复遍历任务。
4. 再增加显式项目模型，避免继续把标签当项目使用。
5. 最后实现看板和保存视图。

### 五、暂不纳入范围

- 多用户登录、角色权限和团队邀请。
- Redis、PostgreSQL、Kubernetes 和实时 WebSocket。
- AI 自动拆解任务、云端同步和复杂第三方集成。
- 一次性迁移到 React/Vue 或复制 Plane 的完整前端架构。

这些能力属于大型协作平台方向，当前阶段引入会增加部署、认证和数据迁移风险，不符合项目“本地可运行、功能可验证”的优先级。

### 六、主要风险与验收检查

- **标签到项目的迁移风险**：同一标签可能被当作分类，也可能被当作项目；迁移时保留标签，不自动删除或强制转换。
- **JSON 兼容风险**：新增字段必须使用 `data.get()` 默认值，不能让旧数据无法读取。
- **日期语义风险**：创建日期适合趋势统计，截止日期适合日历和提醒，两者不能混用。
- **删除风险**：归档、恢复和永久删除要分开，默认操作不应破坏历史数据。
- **回归风险**：每个阶段完成后运行 `python -m pytest tests/ -q`，并手动检查 CLI、任务列表、日历、项目和统计页面。

## 2026-08-04 11:54 更新：阶段 0 和阶段 1 实施结果

### 已完成

- 统一日期解析，支持普通 ISO 时间、日期时间输入和 `Z` 时区格式。
- 日历改为按 `due_date` 展示，周报和统计继续按 `created_at` 统计创建趋势。
- 提醒过滤已完成和已归档任务，只提示未完成任务的临近截止或逾期状态。
- CLI 和 Web 新建/编辑任务均可设置、修改和清除截止时间。
- 任务新增 `updated_at`、`completed_at` 和 `archived` 字段。
- 旧 JSON 缺少新增字段时自动使用兼容默认值。
- 默认任务列表隐藏归档任务，JSON/CSV 导出仍保留归档数据。
- 增加归档、恢复和统一筛选/排序存储方法，为后续归档页面和看板提供基础。
- 新增阶段测试，覆盖旧数据兼容、日期解析、截止日期日历、提醒过滤、归档恢复、CLI/Web 截止时间和查询排序。

### 验证结果

```text
python -m pytest tests/ -q
29 passed
```

### 尚未纳入本轮

- 没有把删除按钮改成归档按钮，避免提前改变原有删除行为；这属于阶段 5。
- 看板、保存视图或 SQLite 尚未纳入；显式项目实体已在阶段 2 实施，后续仍属于阶段 3 至阶段 6。

## 2026-08-04 12:10 更新：阶段 2 实施结果

### 已完成

- 新增轻量 `Project` 实体，项目拥有独立 ID、名称、描述、创建时间和更新时间。
- JSON 存储新增 `projects`、`next_project_id`，旧 `tasks.json` 没有项目字段时仍可读取。
- 任务新增可选 `project_id`；项目存在性在新增和更新任务时校验，未归属项目的任务继续保留。
- 项目列表和项目详情改为读取显式项目，不再根据标签自动生成项目；标签继续承担跨项目分类职责。
- CLI 新增 `create-project`、`list-projects`、`update-project`，任务支持 `--project-id`、`--no-project` 和 `--clear-project`。
- Web 新增项目创建/编辑页面，任务表单支持选择所属项目；项目详情只显示当前项目任务。
- JSON/CSV 导出补充项目数据和任务项目关联，避免导出时丢失关系。
- 新增阶段 2 测试，覆盖实体持久化、项目级隔离、CLI、Web 和导出行为。

### 验证结果

```text
python -m compileall -q models storage commands routes web_app.py main.py
python -m pytest tests/ -q
36 passed
```

### 尚未纳入本轮

- 没有实现保存视图、SQLite/API 或项目删除/归档；看板已在阶段 3 实施。
- 没有把历史标签自动转换为项目，避免改变现有任务的分类语义。

## 2026-08-04 12:25 更新：阶段 3 WeKan 式看板实施结果

### 已完成

- 新增 `/board` 看板入口，按“待办 / 进行中 / 已完成”三列展示未归档任务。
- 看板支持全部项目、指定项目和未归属项目筛选，筛选结果复用存储层项目查询。
- 任务卡片展示优先级、截止时间、项目、标签、描述和任务 ID，保持字段密度清晰。
- 卡片内提供状态选择和提交按钮，可直接把任务移动到其他状态；状态更新继续走 `JSONStorage.update()`，因此会同步维护 `updated_at` 和 `completed_at`。
- 新增响应式三列布局：桌面端并排展示，窄屏横向滚动列，保留看板的状态空间感。
- 看板入口加入主导航，空列、归档任务和项目筛选异常均有明确处理。
- UI 取舍参考 Plane 的工作台分区与视觉层级、Focalboard 的卡片字段组织、WeKan 的列式 Kanban 状态流；没有复制其代码或引入前端框架。

### 验证结果

```text
python -m compileall -q models storage commands routes web_app.py main.py
python -m pytest tests/ -q
41 passed
Flask test client: /board=200, /static/app.css=200
```

### 尚未纳入本轮

- 没有实现拖拽排序、拖拽移动、保存视图和实时协作；当前采用无 JavaScript 的表单状态移动，优先保证可验证和可持久化。
- 当前环境没有 `agent-browser` 或 Playwright，未执行真实浏览器截图检查；已通过模板渲染、路由和静态资源响应检查。

## 2026-08-04 12:38 更新：阶段 4 保存筛选视图实施结果

### 已完成

- 新增 `SavedView` 实体，JSON 顶层数据增加 `saved_views` 和 `next_view_id`，旧数据读取保持兼容。
- 任务查询支持项目、状态组合、优先级、标签、截止日期起止范围、排序字段和倒序条件。
- 新增 `/views` 保存视图工作区，支持临时筛选、结果查看和视图库管理。
- 内置“高优先级未完成”和“本周到期”两个常用筛选预设。
- 支持保存、读取和删除筛选视图；删除视图不会删除任务数据。
- 排序增加任务 ID 作为稳定的次级键，避免相同字段下结果顺序不稳定。
- 日期边界统一解析，截止日期止自动覆盖当天 23:59:59，避免遗漏当天任务。
- 新增阶段 4 测试，覆盖组合筛选、截止日期边界、视图持久化、Web CRUD、预设和非法参数。

### 验证结果

```text
python -m compileall -q models storage commands routes web_app.py main.py
python -m pytest tests/ -q
46 passed
Flask test client: /views=200, /views?preset=week-due=200, /static/app.css=200
```

### 尚未纳入本轮

- 没有实现视图编辑、视图共享、拖拽排序或实时同步；当前保存视图覆盖个人本地筛选复用场景。
- 没有引入 SQLite；JSON 仍是当前默认存储，后续复杂查询增多后再评估迁移。

## 2026-08-04 12:54 更新：阶段 5 归档、恢复、备份与导入校验实施结果

### 已完成

- Web 任务列表的“删除”入口改为归档，CLI `delete-task` 默认归档；只有明确传入 `--permanent` 才会永久删除。
- 新增 `/archive` 归档任务页面，支持查看归档任务并恢复到日常任务列表。
- JSON 存储写入改为同目录临时文件 + 原子替换，降低写入中断导致数据损坏的风险。
- 新增完整备份下载，包含归档任务、项目、保存视图和结构版本信息。
- 设置页面支持 JSON 导入，导入前校验记录结构、ID、任务项目引用、状态和筛选条件。
- 支持 `remap`、`skip`、`replace` 三种 ID 冲突策略，默认重映射冲突 ID 以保留现有数据。
- 导入校验失败或写入失败时回滚内存状态，现有 JSON 文件保持不变。
- 新增阶段 5 测试，覆盖归档恢复、CLI/Web 默认归档、完整备份、非法导入保护、冲突重映射、跳过和覆盖策略。

### 验证结果

```text
python -m compileall -q models storage commands routes web_app.py main.py
python -m pytest tests/ -q
53 passed
```

### 尚未纳入本轮

- 没有实现备份文件的自动定时生成、云端备份或跨设备同步。
- 永久删除仍保留为显式维护操作，默认业务入口不会执行不可逆删除。

## 2026-08-04 13:07 更新：阶段 6 SQLite/API 化实施结果

### 已完成

- 新增 `StorageProtocol`，明确任务、项目、保存视图、归档和 JSON 导入导出的公共存储接口。
- 新增 `SQLiteStorage`，使用 SQLite 表、外键、索引和事务保存任务、项目、保存视图及归档状态；业务方法继续复用 JSONStorage 的规则。
- 新增存储工厂，默认仍使用 JSON；设置 `TASK_MANAGER_STORAGE=sqlite` 后 CLI、Web 和 API 统一切换到 SQLite，路径可通过对应环境变量覆盖。
- 新增 `migrate-sqlite` CLI 命令，将 JSON 数据迁移到独立 SQLite 文件，默认 `replace` 策略可重复执行而不会重复已有 ID。
- 保留原有 JSON 备份、导出和导入能力，SQLite 后端也能复用阶段 5 的校验与冲突处理。
- 新增 `/api/v1` REST API，支持健康检查、任务查询/创建/更新/归档、项目查询/创建及项目详情；API 与 Web/CLI 共用存储业务方法。
- 新增阶段 6 测试，覆盖 SQLite 重启持久化、迁移幂等性、工厂切换、CLI 迁移和 REST API 闭环。

### 验证结果

```text
python -m compileall -q models storage commands routes web_app.py main.py
python -m pytest tests/test_phase_6.py -q
6 passed
python -m pytest tests/ -q
59 passed
```

### 尚未纳入本轮

- SQLite 当前采用内存模型 + 事务快照写入，尚未把复杂筛选全部下推为 SQL 查询。
- API 暂未加入认证、分页、限流和多人协作权限；当前定位仍是本地单用户 API。
- 尚未提供数据库自动备份、云端同步或跨设备冲突合并。

## 2026-08-04 13:12 更新：API 认证与分页实施结果

### 已完成

- 新增可选 Bearer Token 认证，token 通过 `TASK_MANAGER_API_TOKEN` 环境变量注入；未配置时保留本地开发兼容模式。
- 除 `/api/v1/health` 外，配置 token 后的 API 请求都必须携带 `Authorization: Bearer ...`，认证比较使用常量时间比较，错误返回统一 401 结构和 `WWW-Authenticate` 响应头。
- 为任务集合、项目集合和项目详情任务增加 `page`、`page_size` 分页参数，默认 20 条、最大 100 条。
- 统一返回 `page`、`page_size`、`pages`、`total`、`count`、`returned` 元数据，保留 `count` 字段兼容阶段 6 客户端。
- 新增阶段 7 测试，覆盖 token 缺失/错误/正确、健康检查例外、任务分页、项目分页、项目详情分页和非法参数。

### 验证结果

```text
python -m pytest tests/test_phase_6.py tests/test_phase_7.py -q
9 passed
python -m pytest tests/ -q
62 passed
```

### 尚未纳入本轮

- 认证仍是单一静态 token，没有用户、角色、刷新 token 或权限模型。
- 分页当前在业务结果集上切片，SQLite 查询尚未完全下推 `LIMIT/OFFSET`。
- 尚未加入限流、审计日志和 API 版本协商。

## 2026-08-04 13:19 更新：SQLite LIMIT/OFFSET 查询优化结果

### 已完成

- 为存储接口增加 `query_page` 分页查询能力，SQLite 后端直接执行过滤、排序、`COUNT(*)` 和 `LIMIT/OFFSET`。
- 任务查询支持项目、归档、状态组合、优先级、标签、截止日期范围和白名单排序字段，排序的空值顺序与 JSON 后端保持一致。
- 新增 `task_tags` 索引表和 `(tag, task_id)` 索引，按标签查询不再依赖 JSON 文本模糊匹配。
- 项目集合增加数据库分页，项目详情使用数据库分页任务查询和聚合统计，避免为列表统计加载项目全部任务。
- API 检测到 SQLite 分页能力时优先调用 `query_page`，JSON 后端继续走原有全量查询和内存切片。
- 对旧版只有 `tags_json` 的 SQLite 文件增加启动时标签索引回填，保持升级兼容。
- 新增阶段 8 测试，覆盖精确标签过滤、组合条件计数、API 不回退全量查询、项目分页聚合和旧数据库标签回填。

### 验证结果

```text
python -m compileall -q models storage commands routes web_app.py main.py
python -m pytest tests/test_phase_8.py -q
4 passed
python -m pytest tests/ -q
66 passed
```

### 尚未纳入本轮

- SQLite 写入仍采用内存模型加事务快照，后续可以再拆分为按记录更新。
- 复杂全文搜索、统计报表和保存视图查询尚未全部下推 SQL。
- 尚未进行大规模基准测试和并发压测。

## 2026-08-04 22:46 更新：主题模式与深色模式实施结果

### 已完成

- 新增全局主题脚本 `static/theme.js`，支持跟随系统、浅色和深色三种模式，并通过浏览器 `localStorage` 持久化模式选择。
- 设置页面新增四种强调色：默认、玫瑰、海洋和薄荷；强调色同样按浏览器保存，刷新页面后仍能保持。
- 基础模板提供右上角主题快捷按钮，可在系统模式、浅色模式和深色模式之间循环切换，并显示当前模式。
- `app.css` 改为使用 CSS 变量组织页面、窗口、表单、看板、弹窗和设置卡片的颜色，深色模式只切换主题变量即可覆盖主要界面。
- 深色模式增加 `color-scheme`、系统偏好监听、移动端主题操作区适配和主题按钮的可访问名称，降低首屏闪烁和窄屏布局问题。
- 新增阶段 9 测试，验证设置页主题控件、全局主题脚本、深色 CSS 变量和系统媒体查询均能被页面正确加载。

### 验证结果

```text
python -m pytest tests/test_phase_9.py -q
2 passed
python -m compileall -q models storage commands routes web_app.py main.py
python -m pytest tests/ -q
68 passed
```

### 尚未纳入本轮

- 尚未使用真实浏览器做截图级视觉回归，当前通过 Flask 页面渲染、静态资源加载和测试套件验证。
- 主题选择是当前浏览器本地偏好，尚未做跨设备同步或用户账户级主题配置。

## 2026-08-07 10:47 更新：阶段 10 UI 优先开发计划

### 目标

本阶段先把任务管理系统做成“看起来完整、操作路径清晰、适合日常使用”的产品界面，暂不追求业务功能一次性闭环。先确定页面层级、信息密度、交互入口和响应式布局，再逐步把真实数据、存储和 API 接入进去。

### UI 优先原则

- 先设计页面结构、组件状态和交互反馈，再决定数据模型是否需要扩展。
- 允许第一轮使用现有任务数据、模板占位数据或静态演示状态展示界面，不在本阶段新增数据库字段和 API 协议。
- 所有“暂未实现”的操作都要有明确的视觉反馈，例如禁用态、空状态、提示气泡或“即将接入”说明，不能伪装成已经完成。
- 继续沿用当前 CSS 变量、浅色/深色主题和原生 HTML/CSS/JavaScript，避免为了做 UI 原型引入大型前端依赖。
- 每个页面优先适配桌面和移动端两个断点，保证列表、卡片、抽屉、筛选区和导航在窄屏下仍然可读。

### 页面改造顺序

1. **阶段 10.1：Today 工作台**
   - 重新整理首页信息层级，增加“今天、已逾期、即将到期、无截止日期”四个任务区域。
   - 增加顶部快速新建入口、搜索入口、今日任务统计和最近使用的保存视图。
   - 先完成卡片、空状态、加载态、无结果态和深色模式样式；今日任务的真实筛选逻辑后置。

2. **阶段 10.2：任务详情抽屉**
   - 设计从列表、看板和今日工作台打开的统一右侧详情抽屉。
   - 详情区预留标题、描述、状态、优先级、项目、标签、截止日期、子任务、备注和操作历史区域。
   - 第一轮只完成展示、编辑入口、关闭、返回和未保存提示的视觉流程，子任务和历史数据后置实现。

3. **阶段 10.3：看板视觉交互升级**
   - 保留现有三列布局，优化列头、卡片密度、优先级标记、截止日期提醒和空列状态。
   - 增加拖拽中的占位区域、悬停高亮、移动成功提示和移动端替代操作的视觉状态。
   - 第一轮可以只做拖拽演示和状态反馈，不立即写入任务状态；真实拖拽持久化放到后续功能阶段。

4. **阶段 10.4：搜索、快捷操作和筛选入口**
   - 在全局导航增加搜索框或搜索按钮，设计搜索建议、无结果、历史搜索和清空状态。
   - 设计 `N` 新建、`/` 搜索、`B` 看板、`V` 保存视图等快捷键提示面板。
   - 先完成入口、弹层和键盘提示 UI，搜索索引、命令执行和快捷键绑定后置。

5. **阶段 10.5：移动端与可访问性收尾**
   - 优化移动端底部导航、筛选抽屉、详情抽屉、看板横向滚动和按钮触控尺寸。
   - 补充焦点态、键盘导航、`aria-label`、对比度和减少动画设置。
   - 用真实浏览器截图检查浅色、深色、空数据、长标题和窄屏布局。

### 暂不实现的功能

- 子任务的数据库关系、递归任务规则和提醒调度。
- 看板拖拽后的真实状态持久化和排序持久化。
- 全文搜索索引、命令面板业务执行和快捷键写操作。
- 任务详情操作历史、评论、附件和多人协作。
- API 的新字段、用户权限、跨设备同步和实时协作。

### 阶段验收标准

- 每个 UI 子阶段至少有一个可访问页面或可复用组件，不以静态截图替代真实页面结构。
- 主要页面同时覆盖浅色、深色、空数据、长文本、错误提示和移动端布局。
- UI 原型不破坏现有 CLI、Web 路由、JSON/SQLite 存储和 API 行为。
- 新增的交互占位状态必须明确标注未接入的业务能力，避免后续测试误把演示行为当成真实持久化。
- 每完成一个子阶段，再单独决定对应功能是否进入下一阶段实现。

## 2026-08-07 10:56 更新：阶段 10.1 Today 工作台 UI 实施结果

### 已完成

- 新增 `/today/` 页面和 Today 工作台蓝图，使用演示数据渲染“今天要做、已逾期、接下来、还没有安排日期”四类任务区域。
- 新增今日完成进度、四项摘要指标、快速记录占位区、工作流入口和空状态提示，形成桌面端工作台的完整信息层级。
- 主导航新增“今日”入口，工作台可以跳转到已有看板、保存视图和日历页面。
- Today 页面沿用现有 CSS 变量、浅色/深色主题和按钮风格，新增桌面、平板和窄屏布局规则。
- 所有任务勾选、详情、快速记录操作明确显示为禁用或“即将接入”，本轮不修改 Task 模型、JSON/SQLite、API 和任务状态。
- 新增阶段 10 测试，覆盖页面渲染、UI 预览提示和已有导航链接。

### 验证结果

```text
python -m pytest tests/test_phase_10.py -q
2 passed
python -m compileall -q models storage commands routes web_app.py main.py
node --check static/theme.js
python -m pytest tests/ -q
70 passed
```

### 尚未纳入本轮

- 真实任务筛选、今日/逾期计算、快速新建、任务勾选和详情抽屉尚未接入。
- 未进行真实浏览器截图回归：本机没有现成 `playwright-cli`，为避免触发 C 盘下载和全局安装，当前采用 Flask 页面渲染测试、编译检查和静态资源检查。

## 2026-08-07 11:01 更新：阶段 10.2 任务详情抽屉 UI 实施结果

### 已完成

- Today 工作台任务卡的“详情”按钮现在可以打开右侧任务详情抽屉，展示演示任务的标题、所属分组、状态、优先级、截止信息、项目和标签。
- 抽屉新增描述、子任务和操作历史三个信息区，使用清晰的占位状态承载后续真实功能，不伪装成已经持久化的数据。
- 新增遮罩点击、关闭按钮、Esc 关闭、打开后焦点转移和关闭后焦点回到原按钮的交互。
- 新增 `today.js` 负责抽屉状态和演示数据填充，未修改任务模型、存储接口、API 或现有任务编辑路由。
- 抽屉复用现有 CSS 主题变量，支持深色模式、窄屏全宽展示、属性网格折叠和底部操作区响应式布局。
- 阶段 10 测试增加抽屉 DOM、静态脚本、Esc 关闭逻辑和页面资源加载检查。

### 验证结果

```text
python -m pytest tests/test_phase_10.py -q
3 passed
node --check static/today.js
python -m compileall -q models storage commands routes web_app.py main.py
python -m pytest tests/ -q
71 passed
```

### 尚未纳入本轮

- 抽屉中的保存、编辑、勾选子任务、描述编辑和操作历史仍未接入真实业务。
- 任务列表和看板尚未复用 Today 抽屉；当前先在 Today 页面完成交互验证。
- 未进行真实浏览器截图回归，原因与阶段 10.1 相同：本机没有现成 `playwright-cli`，不安装全局依赖或触发 C 盘下载。
